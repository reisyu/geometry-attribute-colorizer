# 変更履歴

このプロジェクトはセマンティックバージョニング(メジャー.マイナー.パッチ)に従います。

- **メジャー**: 互換性が壊れる変更(CSV列構成・対応DXF形式の変更など)
- **マイナー**: 機能追加
- **パッチ**: バグ修正・文言修正

## [1.4.0] - 2026-08-27

### 追加
- **書き出しファイル名にデータ名が付くようになった**。`elevation.svg` のような
  固定名では複数の壁面を扱うとダウンロードフォルダで区別できなかった。
  データ名が空なら従来どおりの名前になる。ファイル名に使えない文字は _ に
  置き換え、制御文字は除去、80文字で打ち切る
- **アスペクト比(Aspect)属性を追加**。幅 ÷ 高さで、1より大きいほど横長。
  細長い石か正方形に近い石かの指標になり、FillRateと組み合わせると形状分類ができる
  - CSVの列が1つ増える(FillRateの次)。過去に書き出したCSVとは列構成が変わる
  - **形状固有の値ではなく向きに依存する**。WidthとHeightが水平/鉛直で
    決まるため、90度回した形では比が逆数になる
  - 高さが潰れた輪郭では空欄にし、Infinityを作らない

### 修正
- **色をリセットするとフィルタの除外表示が効かなくなる問題を修正**。
  半透明での除外表示は色分け用マテリアルにしか効かないため、色を元に戻すと
  「除外: N個」と表示されているのに画面上は1つも除外されない状態になっていた
  (「完全に非表示」モードだけは効いていたため、余計に分かりにくかった)
  - 色のリセット時にフィルタも解除し、解除したことを伝えるようにした
  - 色分け前にフィルタを有効化しようとした場合は、その場で戻して
    先に色分けが必要であることを伝える

## [1.3.1] - 2026-08-14

### 修正(不特定多数の利用を想定した堅牢性)
- **有効な数値が1つも無い属性を選ぶと、凡例に「Infinity 〜 -Infinity」と
  表示されていた問題を修正**。値が全て空欄の列などで発生する。
  着色を行わず、理由(値が空欄か数値として読めない)を伝えて止めるようにした
- **範囲の手動指定に数値として読めない値を入れると、黙って無視されていた**。
  「指定したのに効かない」と誤解されるため、どの入力を無視したかを通知する
- 空ファイルや短すぎるファイルを.glbとして読み込むと、DataViewの英語の
  内部エラーがそのまま画面に出ていたのを、意味の通るメッセージに変更

### テスト
- 壊れた入力とメッセージの検証を追加(36アサーション)。
  壊れたGLB・崩れたDXF・範囲が作れない数値・不正なIDを対象に、
  例外を出さないことに加えて**利用者に意味の通るメッセージが出ること**
  (内部の英語エラーが漏れないこと)を検証する

## [1.3.0] - 2026-08-14

### 読み込み
- **LWPOLYLINE形式のDXFに対応**。AutoCAD R14(1997年)以降の標準的なポリラインで、
  AutoCAD・QGIS・Illustratorなど一般的なCAD/GISツールが既定で書き出す形式。
  従来は POLYLINE / VERTEX のみ対応しており、これらのツールで作ったDXFは
  1件も読み込めなかった(利用者を増やすうえで最大の障壁だった)
  - 標高(コード38)をZ座標として全頂点に適用
  - 閉じフラグ(コード70)で選別し、断面線などの開いた線は輪郭の対象外にする。
    ただし閉じたものが1つも無い場合に限り開いたものを採用する
    (閉じフラグを立てないツールで「0個」の行き止まりにならないようにするため)
  - 円弧(バルジ)は直線として読み込み、件数を通知する
  - 除外・救済・直線化はいずれもステータスに明示する
- 輪郭が見つからない場合のメッセージを改善。対応形式と対処方法を具体的に示す
  (従来は「閉じたポリラインが見つかりませんでした」とだけ出るため、
  LWPOLYLINEのファイルでは原因を切り分けられなかった)

### テスト
- LWPOLYLINE形式の検証を追加(20アサーション)。頂点の組み立て・標高の適用・
  閉じフラグの選別・救済動作・既存形式の回帰を含む

## [1.2.2] - 2026-07-25

### 内部構造(挙動の変更なし)
- SVG書き出し2関数の重複(約150行)を解消。立面図SVGと現在のビューSVGは
  投影の求め方だけが異なりそれ以外は同一だったため、投影の違いを `view`
  オブジェクトに閉じ込め、`buildExportSVG()` / `readSVGExportOptions()` /
  `downloadSVG()` に共通化した。SVG機能の修正が1箇所で済むようになる
- `exportElevationSVG` 185行 → 91行、`exportCurrentViewSVG` 135行 → 68行。
  6行以上の重複は315行 → 131行
- 設定4パターン×2方式+平行投影の計9通りで、リファクタ前後のSVG出力が
  長さ・内容とも完全一致することを確認済み

## [1.2.1] - 2026-07-25

### テスト基盤(重大な欠陥の修正)
- **テストが古いコードを検証していた問題を修正**。`tests/extracted.js` の再生成が
  手動運用だったため、`normalizeId` を常に `"BROKEN"` を返すよう破壊しても
  94件全てが成功する状態だった。`run_all.js` の冒頭で `extract_for_tests.js` を
  必ず実行するようにした
- **test_fitview.js / test_winding.js がロジックの写しを検証していた問題を修正**。
  index.html をどう変更しても成功し続ける状態だった。実コードを検証するため、
  THREE・DOM に依存しない純粋部分を `solveFitDistance` / `solveFitOrtho` /
  `flipTriangleWinding` として index.html 側に切り出し、テストはこれを直接呼ぶ
- 平行投影のフィット(`solveFitOrtho`)と巻き順反転の検証を追加。
  アサーション数 94 → 111 件

### 修正
- モデルを読み込んでも「色分け済み」の状態が残り、未着色のモデルに対して
  実行ボタンが「色分けを再実行」のままになっていた問題を修正
  (`disposeModel` で状態を戻すようにした)

## [1.2.0] - 2026-07-25

### 表示・確認
- 視点プリセットに「上面」(真上から見下ろす)を追加
- 透視投影 ⇄ 平行投影の切り替えを追加。向き・ズームを保ったまま切り替わり、
  「現在のビューをSVGで保存」の書き出し結果にも連動する(平行投影時はパースなし)
- 軸ギズモをビュー右下に常時表示。読み込んだデータのX/Y/Z軸が現在の視点で
  どちらを向いているかが分かる
- 平行投影中はギズモ下にラベルを表示(既定の透視投影では非表示)

### UI(第三者視点レビューによる改善)
- **起動時に「色分けを実行」が最も目立つ問題を修正**: サンプルは起動時点で
  色分け済みなのに、唯一の塗りつぶしボタンが押下を促していた。実行後は
  設定変更が自動反映されボタンは不要になるため、輪郭線ボタンへ格下げし
  文言も「色分けを再実行」に変える
- **連番が「順番にやること」と誤読される問題を修正**: 実際には飛ばせる
  ステップ2・4・5・6の見出しに「(任意)」を明記。必須の流れは 1 → 3 → 7
- 初期展開を必須の1・3のみに変更(仕様書の記述と実装がずれていたものを修正)
- **タップ領域の拡大**: 開閉ヘッダー14〜15px・チェック行16pxと、iPad対応を
  掲げる割に小さすぎたため、いずれもWCAG 2.2の最小である24px以上を確保
- **色見本に文字ラベルを併記**: 何の色かをホバーのtitle属性でしか
  説明しておらず、ホバーの存在しないタッチ環境では判別不能だった
- 文字サイズの階層を整理(16/13/12/11pxの4段階)。アプリ名を16pxに拡大し、
  サブ見出しが配下のラベルより小さいという逆転を解消
- 開閉の見た目を統一(矢印を右端に揃え、記号・サイズ・色・回転を一致)
- ステータス表示を12px・本文色に強調し、日本語で字面が乱れる等幅指定をやめた
- ステップ1にサブ見出し(メインモデル/背景モデル)を追加
- 補足文を`.section-note`(セクション冒頭の説明)と`.hint`(直前のコントロールの補足)に
  書き分け、位置の意味を明確化

## [1.1.1] - 2026-07-24

### UI
- サイドパネル「3. 色分けする」「7. 書き出す」を用途ごとの小見出しで整理し、
  使用頻度の低い設定(範囲の手動指定・カテゴリ数調整・SVGの含める要素/線の太さ)を
  折りたたみ式の「詳細設定」にまとめて初期表示をすっきりさせた
- 「操作ガイド」リンクを説明文の行から独立させ、押しやすいサイズに変更
- ラベルの文言重複・不正確さを修正(「配色」見出しと同名だったフィールドラベルを
  「配色パターン」に変更。カスタムを含むため「プリセット」は不採用)

## [1.1.0] - 2026-07-24

### 書き出し
- 現在のビューをSVGで保存: 現在のカメラの位置・向き・ズームをそのまま使い、
  画面に見えている通りの遠近感(パースあり)でSVGを書き出す新モードを追加。
  立面図SVGと同じ凡例・ヒストグラム・ラベル・傾きラインの設定を共有するが、
  距離により見かけの縮尺が変わるためスケールバーは出力しない

## [1.0.0] - 2026-07-16

最初の正式リリース。

### 読み込み
- DXF(閉じた3Dポリライン)の読み込みと幾何属性の自動計算
- Rhino等が書き出す2Dポリライン形式のDXFにも対応(OCS座標の自動変換、
  同一レイヤー時のID自動一意化)
  (Width / Height / Area / FillRate / Tilt / RectAngle / Flatness / SelfIntersect / X / Y / Z / SrcID / Course)
- GLB(glTF 2.0バイナリ)+外部CSV属性の組み合わせに対応
- フォトグラメトリモデル(OBJ + MTL + テクスチャ)の背景重ね表示。
  複数ファイル同時選択またはフォルダごと選択。テクスチャは最大2048pxに自動縮小
- 原点シフトの共有(worldOffset)により、実座標系のままDXFと背景モデルが位置合わせされる
- 大規模データ対応: パース・属性計算のチャンク処理+プログレスバー
- iPad対応: タッチ操作(1本指回転・2本指パン・ピンチズーム)、ファイル選択、数値キーボード

### ID
- 読み込み時に立面図の左下から自動採番(下の段から、段内は左から右)
- 元IDをSrcID属性として保持(トレーサビリティ)
- 段(コース)番号をCourse属性として保存
- 手動の「IDを振り直す」ボタン

### 色分け・可視化
- 数値属性のグラデーション(赤→青 / 赤→緑 / 黄→青 / グレースケール / カスタム3色)、
  反転、範囲の手動指定、即時反映
- テキスト属性のカテゴリ色: 20色パレット+黄金角による自動生成(色数無制限)、
  出現数順の割り当て、「上位7カテゴリのみ色分け」オプション
- 凡例+ヒストグラム(24ビン、実際のグラデーション色で着色)
- 傾きライン: 外接矩形の水平軸に沿った中心線を矩形回転角の対称グラデーションで表示。
  色・太さ(20〜100mm実寸)のカスタマイズ、面のうねり(Flatness)に応じた浮かせ
- フィルタ: 任意の数値属性×範囲条件(複数・AND)、自己交差除外、半透明/非表示
- ラベル: 複数属性、文字サイズ、距離カリング
- 選択オブジェクトのハイライト(クリックでオレンジの輪郭強調)
- ダーク/ライトテーマ切替

### 書き出し
- CSV(BOM付きUTF-8、RFC 4180準拠のエスケープ)。データ名を設定すると
  全行にDataset列が付与され、複数データの横断分析(BIツール・pandas)で由来を識別できる
- ID付きGLB(法線の向きを壁全体で統一して出力)
- 立面図SVG: 平均法線による正射投影、表裏の自動判定、凡例・ヒストグラム・
  スケールバー・ラベル・傾きラインの埋め込み、要素の取捨選択、
  輪郭線の太さ指定(mm実寸)、塗りつぶしなし(線画)モード

### 操作性
- カーソル位置中心のホイールズーム(不動点の相似変換)
- 回転開始時のピボット自動補正(画面中央のオブジェクト表面へ)
- ダブルクリック/ダブルタップでフォーカス
- 視点プリセット(正面=立面図と同じ向き / 全体)
- 全体フィットのリセンタリング方式(余白5%、斜め視点でも均等)
- アコーディオンUI+ヘッダーへの設定状態サマリー表示

### 品質
- 幾何計算はNode.jsによる単体テスト(実コード抽出方式)と
  実データ統合テストで検証済み(tests/ 参照)
- リリース前の第三者視点レビューで以下を修正:
  - モデル再読み込み時にジオメトリ・マテリアルがGPUメモリに残り続けるリークを修正
    (disposeModelの導入。大規模データの読み直しを繰り返しても安定)
  - ファイル読み込み中に別の読み込みを開始でき、内部状態が破損しうる競合を修正
    (読み込みロックの導入。処理中は「完了までお待ちください」と案内)
  - 色計算関数にNaN/Infinityの防御を追加(通常経路では到達しない純関数レベルの保証)
