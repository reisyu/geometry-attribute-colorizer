# 変更履歴

このプロジェクトはセマンティックバージョニング(メジャー.マイナー.パッチ)に従います。

- **メジャー**: 互換性が壊れる変更(CSV列構成・対応DXF形式の変更など)
- **マイナー**: 機能追加
- **パッチ**: バグ修正・文言修正

## [1.24.0] - 2026-09-04

### 追加
- **現在地の目安を俯瞰図に表示できるようにした**(ビュー右上の「現在地」)。
  石垣のどのあたりに立っているかを、誤差込みで示す
  - **石の特定には使えない**。GPSの誤差は屋外で5〜20m、石垣の際では
    20〜50mまで落ちる。答えるのは「壁のどのあたりか」だけ
  - **精度円を必ず一緒に描く**。点だけを出すと、その点を信じて別の区画を
    記録してしまう。円が壁の半分を覆っていれば、それが実態
  - **座標系(平面直角座標系19系)を自動で判定する**。現在地を全系で変換し、
    データに最も近いものを選ぶ。CADでよくある軸の入れ替え(x=東, y=北)も
    同時に判定する
  - 近い候補が無ければ**何も表示しない**。別の場所のデータか、平面直角座標
    ではないということなので、当てずっぽうの位置は出さない
  - 「近くの区画を見る」で、現在地に最も近いオブジェクトへ視点を寄せられる
- 地図タイルは読み込まない。「地図」はデータ自身の俯瞰図(石を上から見たもの)。
  外部通信はこのアプリの約束に反し、オフライン版でも動かなくなるため

### 制約
- **オフライン版(ファイルを直接開く形)では使えない**。ブラウザが安全な接続
  (https)でのみ位置情報を許可するため。押したときに理由を出す
- 位置は端末内だけで扱い、外部に送らない。CSVにも記録しない
- 追従はしない(ボタンを押したときに1回取得)。標高は使わない

## [1.23.4] - 2026-09-03

### 修正
- **分類だけを入力した状態で、畳んだ「7. 書き出す」の見出しに未保存の表示が
  出なかった**。ビュー上のバッジとパネル内の注意は「手入力20件 未保存」と
  出ているのに、ステップを畳むと消える状態だった
  - 未保存の件数を数える場所が3箇所に分かれており、見出しだけがメモの件数を
    数えていた。**1つの関数(`unsavedManualSummary`)にまとめた**

### 変更
- 畳んだ「2. 属性データ」の見出しに、分類の列の数を出すようにした
  (例: 「123行 / 分類2列」)。作った列があることが畳んだ状態でも分かる

## [1.23.3] - 2026-09-03

### 修正
- **書き出したSVGの凡例に「未入力」の行が無かった**。画面の凡例にだけ足していたため、
  図の灰色のオブジェクトを説明する行が図から消えていた(§8.6.1と同じ型の食い違い)
  - あわせて、SVGの凡例にもカテゴリごとの**件数**を出すようにした。
    画面の凡例と件数まで含めて同じ文字列になる

### 変更
- 配布用の1枚手順書(`quickstart.html`)に、まとめて選ぶ(ブラシ)と分類を追記した。
  「メモは計算し直せない唯一のデータ」という記述も、分類を含む形に直した
  (A4 1枚に収まることを確認済み: 印刷時288.9mm)
- 操作ガイド・READMEの記述を現在の動作に合わせた
  (未入力の凡例の文言、カテゴリ色が使い始めた順で固定されること、
  選択モードを抜けると選択が解除されること)

## [1.23.2] - 2026-09-03

### 修正
- **分類の値を1つ足すと、既に色が付いていた値の色が入れ替わっていた**。
  花崗岩だけのときは花崗岩が赤、そこに砂岩を足すと砂岩が赤・花崗岩が青になる。
  色を**出現数の多い順**に割り当てていたため、件数が逆転した瞬間に順番が動いていた
  - **使い始めた順に色を固定**するようにした。新しい値は末尾に足されるので、
    既にある値の色は変わらない
  - 一度消えた値も並びから外さない(色番号を空けたままにし、戻ってきたら同じ色に戻る)
  - 「上位7カテゴリのみ色分け」の**上位は従来どおり出現数**で決める
  - 属性ごとに並びを持つため、石種とエリアを行き来しても色が変わらない

## [1.23.1] - 2026-09-03

### 変更
- **選択モードを抜けたときに、選択を解除する**ようにした(v1.22.0では保持していた)。
  通常モードには解除する手段が画面に出ておらず、残したままだと減光と縁取りだけが
  残って理由が分からなくなるため。何個解除したかは画面に出す
  - その代わり、回転したい場合は選び直しになる。値やメモを付けてから抜けること

### 修正
- **「分類の列」の入力欄が潰れていた**。ボタンが横幅いっぱいを占め、
  入力欄が17pxまで縮んでいた。入力欄とボタンを別の行に分けた

## [1.23.0] - 2026-09-03

### 追加
- **石種・エリアなどの分類を、手で付けられるようにした**。ステップ2で分類の列を作り、
  ブラシで選んだオブジェクトにビュー上のパネルから値を付ける
  - **選択肢は事前に決めない**。その列に実際に使われている値がそのまま候補になり、
    新しい値は入力した瞬間に候補に加わる。現場で新しい石種が出ても手が止まらない
  - 語彙の保管場所は**CSVそのもの**。前回の調査のCSVを読み込めば候補が戻る
  - 値は付けた直後から色分け・ラベル・フィルタの対象になる
- **値の表記ゆれを後から直せるようにした**(ステップ2)。値ごとの件数が見え、
  名前を変えれば全行に反映される。既にある値に変えれば**統合**になる
  - ゆれは必ず起きる。起きない仕組みより、起きた後に直せる仕組みを用意した
  - 入力時にも前後の空白と全角英数だけは揃える(漢字の異体字は機械では揃えられない)
- 自動計算される列と同じ名前の列は**作らせない**。その名前で作ると、
  DXFを読み直したときに計算値で上書きされ、手で入れた値が消える

### 変更
- **テキスト属性の未入力(空欄)を、カテゴリではなく灰色**にした。凡例にも
  「未入力(n)」として出る。分類の途中では空欄が最も多く、色を1つ食うだけでなく
  **まだ付けていない場所**が読めなくなるため(数値属性の空欄と同じ扱いに揃えた)
- **未保存の警告とブラウザへの控えを、メモ以外の手入力にも広げた**。
  メモだけを対象にしたままだと、石種を40個に付けて閉じた瞬間に消える
  - 未保存の表示は、分類も使っている場合「手入力○件」に変わる
  - 以前の形式(メモだけ)の控えもそのまま読める

## [1.22.1] - 2026-09-03

### 修正
- **選択モードでクリックすると、押している間だけ選ばれて離すと解除されていた**。
  押した瞬間にブラシが1個拾い、離した瞬間のタップ処理が同じものを
  「もう選ばれている」と見て外していた(足して外すで元に戻る自己衝突)
  - **押した時点では塗らない**ようにした。塗りは最初の移動から始める。
    押した位置は始点として残るので、なぞったときの取りこぼしはない
  - わずかに動いてブラシが拾っていた場合は、離したときに足し引きしない。
    マウスは数pxの揺れが必ず出るため、「動いていない=タップ」と
    「動いた=塗り」の境目で両方が走ると同じことが起きる
  - 取り消しの控えは「最初の1個を足す直前」に1回だけ取るようにした。
    押すたびに取ると、何も塗らなかったときに取り消しが空振りする
- **フィルタで完全に非表示にしたオブジェクトが、タップでは選べてしまっていた**。
  Three.js r128のレイキャストは`visible = false`を無視して当たりを返す(実測)。
  ブラシ側は除外していたため、同じモードの中で拾い方が食い違っていた。
  選ばれても画面に出ないまま件数だけ増える状態だった
  - 通常モードのクリックでも同じことが起きていたので併せて直した
- **選択が0個に戻っても、最後の1個の属性表とメモ欄・強調表示が残っていた**。
  「0個選択中なのに1個光っている」状態になるため、選択モードでは畳むようにした

## [1.22.0] - 2026-09-03

### 追加
- **なぞって複数のオブジェクトを選べるようにした(ブラシ)**。ビュー右上の
  「選択モード」に切り替えると、左ドラッグ(1本指)が筆になる
  - 選ぶ対象は「孕み出している一角」「積み直しの範囲」のように、
    **属性では表せないまとまり**。数値で表せる集合ならフィルタで足りるうえ、
    そもそもメモを書く必要がない(§6.8)
  - **奪うのは回転だけ**。パン(右ドラッグ・2本指)とズーム(ホイール・ピンチ)は
    通常モードのまま使える。選択は分単位で続く作業なので、途中で見る場所を
    変えられないと使えない
  - **選択はモードを抜けても残る**。回転したくなったら一度抜けて戻ればよい
  - 筆の中心の真下にあるものと、筆の円(半径16px)に重心が入ったものを拾う。
    片方だけだと「大きな石の端をなぞったのに入らない」「隣を巻き込む」が起きる
  - 壁が曲がっているときに**裏側の面を拾わない**。画面に見えていないものが
    黙って混ざるのを防ぐ
- **選択したすべてに同じメモを書けるようにした**。2つ以上選ぶと右上のパネルが
  一括入力に切り替わる
  - **黙って上書きしない**。既にメモがある個数を先に示し、ボタンを押すまで
    何も書き換えない
- 選び間違いに備えて、**1回のなぞりを単位に取り消せる**ようにした(20回分)。
  選択モード中は空きをタップしても解除しない(押し間違いで消えないため)

### 変更
- 選択中は、選んでいないものを薄くして範囲が見えるようにした。
  縁取りだけでは数十個を選んだときに線だらけになって読めない

## [1.21.1] - 2026-09-03

### 変更
- **選択の輪郭線の太さを3px固定にし、設定項目をやめた**。v1.21.0では
  ステップ4(傾きライン)にスライダーを置いたが、**流れのどこにも属さない設定**を
  既存のステップに間借りさせる形で、傾きラインを使わない利用者には
  見つからない場所に隠れていた
  - 1pxでは写真の上で見つけられず、太すぎると石そのものを隠す。
    3pxが両方を満たすため、利用者に決めさせず固定する
  - 設定をどのステップに置くかの判断基準を仕様書に明記した(§10.7)
- **傾きラインの線の太さの初期値を 50mm → 75mm** にした。
  既に設定を保存している場合はこれまでの値のまま

## [1.21.0] - 2026-09-03

### 変更
- **背景モデル(フォトグラメトリ)と輪郭モデルの前後関係を統一した**。
  輪郭モデルは平らな板なので、凸凹した写真の表面と同じ場所を奪い合い、
  どちらが手前かがピクセルごとに入れ替わって**色がまだらに散っていた**。
  石が手前に膨らんでいる部分では板が写真に潜り込むため、深度の微調整
  (`polygonOffset`)では直らない
  - 描画を「背景 → 深度を捨てる → 調査対象」の2パスに分け、
    **輪郭モデルを常に手前**に描くようにした
  - 背景モデルを重ねている間は輪郭モデルを半透明(既定65%)にし、
    下の写真が透けて見えるようにした。濃さはステップ1のスライダーで変えられる
  - 傾きラインと選択の強調も、半透明の石に隠れないように順番を揃えた

### 追加
- **選択したオブジェクトの輪郭線を太くできるようにした**(ステップ4、既定4px)。
  従来は線が常に1pxで、写真の上では見つけられなかった
  - WebGLは線の太さの指定を無視する(常に1px)ため、線分ごとに板を張り、
    頂点シェーダで画面上の太さに広げている
  - **実寸ではなく画面のpx指定**にした。「どこを選んだか気づくため」の表示なので、
    ズームしても太さが変わらないほうが目的に合う(傾きラインは実寸mmのまま)
- 輪郭モデルの不透明度と選択の輪郭線の太さも、他の作業環境の設定と同じく
  ブラウザに保存するようにした

## [1.20.1] - 2026-09-03

### 修正
- **CSVを読み込み直した後に書いたメモが、どこにも残らないことがあった**。
  オブジェクトとCSVの行の対応が古いCSVのまま残るため、色分けをやり直すまで
  **捨てられる行にメモが書かれていた**。画面のメモ欄には文字が出ているのに、
  CSVにもブラウザの控えにも残らない
  - CSVを読み込んだ時点で対応を作り直すようにした
  - 万一それでも古い行を掴んだ場合は、メモ欄を書けない状態にして理由を出す
  - インスペクタが前のCSVの属性を表示し続ける問題も同時に解消した
- **メモを書いた直後にページを閉じると、ブラウザの控えに入らなかった**。
  保存は500ms待ってからまとめて書くため、警告を無視して閉じると復元もできない
  - 閉じる直前と、画面が隠れた時点で必ず書き切るようにした
    (スマホではページを閉じる合図が出ないことがあるため両方で受ける)
- **元ID(SrcID)を持たないデータでは、メモを控えられないことが伝わらなかった**。
  GLB+外部CSVの利用では自動保存が働かないが、画面上は普通に見えるため、
  控えがあるつもりで失われる。データごとに1回だけ知らせるようにした
  - IDで代用はしない。振り直しで別のオブジェクトに付くため(§6.3.2)

## [1.20.0] - 2026-09-02

### 修正
- **書き出したCSVを読み戻すとき、DXFを作り直していると別のオブジェクトに
  値が付いていた**。表示上のIDは立面図の左下から自動採番されるため、石が
  1つ増減しただけで番号が全部ずれる。IDで突き合わせていたので、メモや属性が
  隣の石に付き、**画面上は正常に見えるため気づけない**状態だった
  - **元ID（SrcID）で突き合わせる**ようにした。元データ由来の値なので
    振り直しの影響を受けない
  - **ずれていたら必ず知らせる**。何個ずれていたかを色分けの完了時に出す
  - SrcID列が無いCSV、値に重複があるCSVは従来どおりIDで照合する

### 変更
- メッシュとCSVの行の突き合わせを1箇所（`makeRowLookup`）にまとめた。
  色分けとフィルタに同じコードが写されており、片方だけ直すと食い違う形だった

## [1.19.1] - 2026-09-02

### 修正
- **「メモ○件 未保存」のバッジが「サンプルデータを表示中」のバッジと
  重なっていた**。左上のバッジを縦積みにまとめ、重ならないようにした

## [1.19.0] - 2026-09-02

### 追加
- **メモが未保存であることを3箇所で示すようにした**。書き出しが唯一の保存手段
  なので、未保存に気づけないことが最大のリスクになる
  - **ビュー左上に「メモ○件 未保存」のバッジ**。パネルを畳んでいても見える
  - **「7. 書き出す」の見出しにも件数**。畳んだ状態でも見える
  - **「属性をCSVで保存」ボタンが目立つ色に変わる**。どこを押せばよいかが
    警告と同時に目に入る
  - いずれも件数を出す。書き出すと3箇所とも消える

### 修正
- **ステップ7を畳むと「メモが未保存です」の注意ごと消えていた**。
  唯一の告知が見えなくなる状態だった

## [1.18.0] - 2026-09-02

### 追加
- **メモをブラウザに自動保存し、読み込み直したときに復元を提案するようにした**。
  これまではページを再読み込みするとメモが消えていた。現場での中断・
  バッテリー切れ・タブの破棄で失われると戻せないため
  - **黙って復元しない**。保存した時刻・データ名・件数を見せたうえで選ばせる
  - **同じデータのときだけ提案する**（データ名・件数・元IDの並びで照合）。
    別のデータに前回のメモが混ざると、番号が偶然合っただけの石に所見が付き、
    画面上は正常に見えるため気づけない
  - **照合は元ID（SrcID）で行う**。表示上のIDは自動採番でずれるため
  - 対応するオブジェクトが見つからなかった分は、件数と元IDを通知する
  - **控えはデータごとに別に持つ**。同じ日にA壁とB壁を回っても、後から書いた
    ほうが先の控えを消すことはない。件数の上限は設けていない
  - **ブラウザの保管領域があふれたときだけ**、古い控えを消す。そのときは
    どのデータの控えを消したかをデータ名と日付で知らせる。黙って消さない
  - **復元しても「メモが未保存です」の表示は消えない**。ブラウザの控えは
    確実な保管ではないので、CSVで保存するまでは未保存として扱う

## [1.17.0] - 2026-09-02

### 変更
- **「メモの有無」で色分けしたときの配色を固定した**。記入済みが赤、未記入は
  グレーになる。これまではテキスト属性の既定どおり出現数順に色を割り当てて
  いたため、**大多数を占める「なし」に赤が付き、注目すべきでない側が最も
  目立つ**状態だった。凡例の並びも「あり」を先にした
  - グレーは「その他（注目対象外）」に使っている色と同じものを使っている
  - この固定は「メモの有無」に限った特例で、他のテキスト属性は従来どおり

## [1.16.1] - 2026-09-02

### 修正
- **メモを書いても、属性インスペクタの「メモの有無」が「なし」のままだった**。
  属性の表は石を選んだ瞬間にしか作られず、メモを書いても作り直されていなかった。
  裏の値は正しく更新されているため色分けもCSVも正しく、**画面の文字だけが
  食い違う**状態だった（すぐ下のメモ欄には本文が入っていて、石も「あり」の色に
  なっているのに、表だけ「なし」と出る）。自分の入力が効いていないと誤解しかねない

## [1.16.0] - 2026-08-30

### 追加
- **「メモの有無」で色分けできるようにした**。メモを書くと `HasNote` 列
  （あり／なし）が自動で作られ、2色で塗り分けられる。どこに所見を残したかが
  図で一望でき、立面図SVGにも出せる
  - メモ本文そのものは色分けに向かない（値が全件異なり、石の数だけ色が生成される）。
    有無だけを派生列として切り出した
  - 全行に必ず値を入れている。空欄にすると欠損値として色分けから飛ばされ、
    「なし」なのか「未計算」なのか見分けられなくなるため
  - 書き出したCSVを読み込み直したときも作り直すので、往復しても使える
- **メモをラベルの表示項目に追加した**。長いメモは改行を空白に潰して先頭12文字＋
  「…」で表示する（全文はインスペクタで読む）。3Dビューと立面図SVGの両方に効く

### 変更
- `extract_for_tests.js` が文字列・数値の定数も切り出せるようにした。
  これまでは配列の定数しか抽出できなかった

## [1.15.0] - 2026-08-30

### 追加
- **メモ欄を追加した**。オブジェクトを選ぶと属性インスペクタにメモ欄が出て、
  点検の所見などを書き留められる。内容はCSVのNote列として書き出され、
  そのCSVを読み込み直せば戻る
  - 複数行が書けるほか、iOS/Androidの標準キーボードの**音声入力**が使える。
    現場で打つより速い
  - 列は最初に書き込んだときに作る。使っていなければCSVに列は増えない
  - **メモはDXFから再計算できない唯一のデータ**なので、失わせないために
    「メモが未保存です」を表示し、ページを閉じるときとファイルを読み込むときに
    確認する
  - 自由記述は値がほぼ全件異なるため、色分け・ラベル・立面図・まとめ書き出しの
    対象からは外している(石の数だけ色が生成されて意味を成さないため)

## [1.14.0] - 2026-08-30

### 変更
- **自己交差(SelfIntersect)を0/1の二値から、交差の大きさを表す0〜0.5の実数に変えた**。
  DXFの輪郭は凹凸のあるメッシュに投影して作られるため、輪郭が視線方向に折り返して
  いる石では投影の結果として小さな交差が出る。二値ではこれと「頂点の順序が壊れた
  輪郭」を区別できなかった
  - 値は、交差点で分かれる2つのループのうち小さい側が全体に占める面積比
  - **Areaはこの値の2倍だけ小さく出る**ため、しきい値を「許容できるAreaの誤差÷2」で
    決められる。おすすめフィルタの 0.01 は「Areaを約2%以上狂わせる交差」を指す
  - 実測では、投影由来の交差は面積比3%以下、順序が壊れた輪郭は中央値12.6%で、
    両者は桁で分かれる
  - CSVの列構成は変わらないが、**書き出した値の意味が変わる**。
    以前のCSVで `SelfIntersect == 1` として集計していた場合は読み替えが必要
- **フィルタの「自己交差しているオブジェクトを除外」チェックボックスを廃止**し、
  通常の条件(残す範囲)として指定する形に統一した。しきい値をチェックボックスでは
  表現できないため。「おすすめ設定を適用」に「SelfIntersect 0.01以下」を追加した

### 修正
- **離れた辺どうしの自己交差を見逃していた**。判定から除外する範囲を輪郭上の距離で
  決めていたため、12頂点以上の輪郭では交差の14〜16%(全体では4.9%)が検出されて
  いなかった。向きの判定を三値(-1/0/+1)にして、除外を頂点の共有だけに絞った
- **閉じたポリラインの終端に始点を書き出すDXFで、全オブジェクトが「自己交差あり」に
  なっていた**。長さ0の辺で外積が0になり、二値の判定が「負」へ倒れていた。
  自己交差フィルタを掛けると全件が消える状態だった

## [1.13.1] - 2026-08-29

### 修正
- **WebGLが使えない環境でも「静かに壊れる」状態になっていた**。画面は普通に
  表示されるのにサンプルが出ず、ファイルを読ませても無反応で、原因を知る
  手がかりが無かった(CDNを読み込めない場合と同じ現象)
  - ハードウェアアクセラレーションを切っている、リモートデスクトップや
    仮想環境でGPUを使えない、といった場合に起こる
  - 原因と対処を示して止めるようにした。起動時の案内は共通化した
    (`showStartupError`)
- **日本語フォントの指定にNoto Sansを追加した**。Windows(Yu Gothic)と
  macOS(Hiragino)しか挙げておらず、Linuxでの表示がフォント設定任せだった

## [1.13.0] - 2026-08-29

### 追加
- **外部ライブラリを読み込めなかったときに、原因が分かる画面を出すようにした**。
  Three.jsとPapaParseはCDNから読み込んでいるが、ネットワーク制限やオフラインで
  取得できないと、**画面は普通に表示されるのに何も動かない**状態になっていた
  (サンプルが出ず、ファイルを読ませても無反応)。原因と対処を示して止める
- **オフライン配布版を作るツールを追加した**(`tools/build_offline.js`)。
  ライブラリを本体に埋め込んだ単一HTMLを作る。ネットワークが当てにならない
  会場での講習などに使う。操作ガイドと練習用DXFも同じフォルダに出力する
  - 公開版はCDN方式のまま。配布用の保険であり常用はしない

## [1.12.2] - 2026-08-29

### 修正
- **CSVを差し替えても属性の紐づけが作り直されず、フィルタが古いCSVの値で**
  **絞り込んでいた**。画面には新しい列名の条件が出ているのに、評価しているのは
  前のデータという状態で、色分けをやり直すまで直らなかった
  - フィルタを使う直前に紐づけを作り直すようにした

### 変更
- **面積の色分け範囲の初期値を 0.05〜1 にした**(従来は0.1〜1)。
  おすすめフィルタの下限(0.05以上)と揃え、
  「フィルタで残る石」と「色が付く範囲」の基準を一致させる

## [1.12.1] - 2026-08-29

### 修正
- **色をリセットした後にSVGを書き出すと、前回の色分けの凡例が付いていた**。
  灰色一色の図にグラデーションの凡例だけが残り、図と説明が食い違っていた。
  リセット時に凡例の状態も捨てるようにした
- **ZIPの中のファイル名に、CSVの列名をそのまま使っていた**。列名は利用者の
  ファイル由来で任意の文字を含む。`幅/高さ` のような列名はZIPの中でフォルダになり、
  `..` を含む列名は展開先の外に書き出されうる状態だった
  - データ名に使っていた整形処理(`safeFileNamePart`)を列名にも通すようにした
  - Windowsの標準機能で、平らに展開されることを確認済み
- **属性の違うCSVに差し替えると、フィルタ条件が黙って別の属性に付け替わっていた**。
  数値だけが残るため、例えば「面積 0.05以上」が「別の属性 0.05以上」に化け、
  画面上は同じ数字が並んでいるので気づけない。全件が除外されることもあった
  - 属性が無くなった条件は付け替えず「(この属性はありません)」として保持し、
    **何を解除したかを知らせる**ようにした
- **全てのオブジェクトがフィルタで除外されているとき、立面図SVGの書き出しが**
  **「DXF/サンプルから読み込んだモデルでのみ利用できます」と誤った理由を出していた**。
  フィルタが原因である旨を伝えるようにした

## [1.12.0] - 2026-08-29

### 追加
- **見た目の設定をブラウザに保存するようにした**。次に開いたときも引き継がれる。
  毎回チェックを入れ直す手間をなくすため
  - 対象: 背景の不透明度、傾きラインの色と太さ、ラベルの文字サイズ・表示距離・
    距離カリング、除外の表現(半透明/非表示)、SVG書き出しの含める要素と
    輪郭線の太さ、まとめ書き出しに線画を含めるか、テーマ、投影方式
  - **色分けの設定(配色・範囲・反転・カテゴリ数)は保存しない**。誰が開いても
    同じ配色になるという前提を崩さないため。保存すると、本人には「前回の続き」に
    見えたまま他の人と色の意味がずれ、ずれていること自体に気づけない
  - データに紐づくもの(データ名・ID列・対象属性・フィルタの条件)と、
    表示のオン/オフ(傾きライン・ラベル・背景モデル)も保存しない
  - 保存先はそのブラウザの中だけ。外部には送信しない。
    プライベートモードなど保存できない環境でも、これまでどおり動作する

## [1.11.0] - 2026-08-28

### 変更
- **色分けしていなくてもフィルタを使えるようにした**。「形だけを絞り込んで見たい」
  という使い方があり、色分けを前提にする必然性が無かったため
  - 属性値の紐づけを色分けから独立させた(`buildAttributeMap`)
  - 除外表示(半透明)は、色分け由来でないマテリアルを**複製してから**書き換える
    (`dimmableMaterial`)。読み込んだGLBのマテリアルは複数のオブジェクトで
    共有されていることがあり、直接触ると関係のないものまで薄くなるため
  - これに伴い**「色をリセット」してもフィルタを解除しなくなった**。
    おすすめ設定も色分けなしで押せる
- **範囲の設定を最初から開いた状態にした**。既定で範囲を固定している(v1.9.0)のに、
  その値が折りたたまれていて見えなかったため
- **まとめて書き出すボタンを他の書き出しボタンと同じ並びに出した**。
  折りたたみの中にあって気づきにくかった。対象の属性を選ぶ部分だけを
  折りたたみに残している
- **おすすめフィルタの面積の最小値を 0.05 にした**(従来は0.1)

## [1.10.0] - 2026-08-27

### 追加
- **まとめて書き出す(ZIP)** を追加した。チェックした属性ごとの立面図SVGを
  一度に書き出し、ZIPファイル1つにまとめて保存する。報告書には属性ごとの図が
  何枚も必要になるが、1枚ずつ属性を切り替えて保存するのは手間が大きいため
  - すべて同じ向き・同じ縮尺の立面図なので、並べて見比べられる
  - **傾きラインの線画**(塗りつぶしなし・色分けなし)も含められる。
    石の積み方を読み取るための図
  - 既定でチェックするのは幅・高さ・面積・矩形充填率・アスペクト比・傾斜角。
    矩形回転角は傾きラインの線画で読み取れるため外してある
  - 書き出しが終わると、画面は元の状態(属性・範囲・色分け)に戻る
  - **外部ライブラリは追加していない**。ブラウザ標準のCompressionStreamで
    圧縮し、ZIPの構造は自前で組み立てている(SVGは約30%まで縮む)

### 変更
- **幅・高さの範囲の初期値を 0.1〜1.5 にした**(従来は上限1.5のみ)。
  下限も決まったことで、極端に小さい値による色の偏りがなくなる
- READMEの見出し画像を新しい初期値で作り直した

### 内部
- 立面図SVGの組み立てと保存を分離した(`buildElevationSVG`)。
  まとめ書き出しが、画面のチェックを書き換えずに別の設定で呼べるようにするため
- `applyColorize()` が成否を真偽値で返すようにした

## [1.9.0] - 2026-08-27

### 変更
- **「範囲を手動で指定」を既定でオンにした**。誰が開いても同じ配色になるため、
  書き出したSVGを並べて比較できる。データに合わせたい場合はチェックを外す
  - 範囲からはみ出す値があるときは凡例のヒストグラムの端が高くなるため、
    見れば分かる。警告は出さない

### 追加
- **範囲が成立しないときに確認ダイアログを出すようにした**。最小値が最大値を
  上回る場合（データが既定値を大きく超えるときに起きる）、全て同じ色になるうえ
  ヒストグラムも山にならず原因に気づけないため
  - 「データに合わせる」を選ぶと入力欄を実際の値に書き換える
  - 「指定のままにする」を選ぶとそのまま描き、以後その属性では聞き直さない
  - **設定が勝手に変わることはない**。どちらにするかは必ず利用者が選ぶ

## [1.8.0] - 2026-08-27

### 変更
- **内蔵サンプルを実際の石垣調査データに差し替えた**（123石）。
  従来は手続き的に生成した理想的な形（ほぼ同寸の5〜7角形を6段に規則配置）で、
  実際の石垣とかけ離れていたため、属性の分布が現実と合っていなかった
  - 同梱の `sample_ishigaki.dxf` から輪郭座標だけを抜き出して埋め込む
    （DXF全文390KBではなく座標のみ24KB。単一HTMLファイルの方針を保つため）
  - 範囲の初期値（v1.7.0）との相性が改善した。従来のサンプルでは初期値を
    適用すると色の広がりが 0.807 → 0.513 に落ちていたが、実データでは
    0.807 のまま変わらない

### テスト
- テスト用に複製していた `tests/data/sample_ishigaki.dxf` を廃止し、
  同梱のサンプルDXFを直接参照するようにした。同名で内容の違うファイルが
  2つある状態を解消し、テストがより現実的な形状（123輪郭・平均8.8頂点）を
  検証するようになった

## [1.7.0] - 2026-08-27

### 追加
- **色分けの手動範囲に、よく使う属性の初期値を用意した**。毎回同じ基準で
  グラデーションを描けるため、別の壁面と見比べるときに色の意味が揃う
  - 幅・高さは上限1.5、面積0.1〜1、矩形充填率0.5〜0.9、
    アスペクト比0.25〜4、傾斜角30〜90、矩形回転角-45〜45
  - 上記以外の属性は空欄（自動）から始まる
  - 値は自由に変更でき、欄を空にすればその属性は自動に戻る

## [1.6.0] - 2026-08-27

### 追加
- **色分けの手動範囲を属性ごとに覚えるようにした**。属性を切り替えて
  戻ってきても、その属性に設定した範囲が復元される
  - 別のモデルを読み込むとリセットされる（範囲は元データに固有の値のため）

### 修正
- **属性を切り替えると前の属性の範囲が残り、色分けが機能しなくなる問題を修正**。
  手動範囲が全属性で共通の1組しか無かったため、例えば幅の 0.27〜0.86 を
  残したまま傾斜角（実際は67〜76）に切り替えると、全ての値が範囲外に
  振り切れて**すべてのオブジェクトが同じ色**になっていた。
  凡例にも前の属性の範囲が表示されるため、原因に気づきにくい状態だった

## [1.5.0] - 2026-08-27

### 追加
- **おすすめフィルタ設定を追加**。調査でよく使う3条件（面積 0.1以上 /
  アスペクト比 0.25〜4 / 傾斜角 30以上）を「おすすめ設定を適用」ボタンで
  まとめて入れられる。小さすぎる破片・極端に細長い形・水平に近い面を除く組み合わせ
  - 既存の条件は置き換える（足し込むと意図しない絞り込みになるため）
  - データに無い属性は飛ばし、飛ばしたことを通知する
  - フィルタ本体と同じく色分けが前提。未実行なら理由を伝えて何もしない

### 修正
- **フィルタの入力欄のラベルを「除外条件」から「残す範囲」に変更**。
  入力するのは残す範囲で、その外側が除外される仕様だが、
  以前のラベルは除外する範囲を入れる欄とも読め、意図と逆の値を
  入れてしまいやすかった

## [1.4.0] - 2026-08-27

### 追加
- **書き出しファイル名にデータ名が付くようになった**。`elevation.svg` のような
  固定名では複数の壁面を扱うとダウンロードフォルダで区別できなかった。
  データ名が空なら従来どおりの名前になる。ファイル名に使えない文字は _ に
  置き換え、制御文字は除去、80文字で打ち切る
- **アスペクト比(Aspect)属性を追加**。幅 ÷ 高さで、1より大きいほど横長。
  細長い石か正方形に近い石かの指標になり、FillRateと組み合わせると形状分類ができる
  - CSVの列が1つ増える(FillRateの次)。過去に書き出したCSVとは列構成が変わる
  - **形状固有の値ではなく向きに依存する**。WidthとHeightが水平/鉛直で
    決まるため、90度回した形では比が逆数になる
  - 高さが潰れた輪郭では空欄にし、Infinityを作らない

### 修正
- **色をリセットするとフィルタの除外表示が効かなくなる問題を修正**。
  半透明での除外表示は色分け用マテリアルにしか効かないため、色を元に戻すと
  「除外: N個」と表示されているのに画面上は1つも除外されない状態になっていた
  (「完全に非表示」モードだけは効いていたため、余計に分かりにくかった)
  - 色のリセット時にフィルタも解除し、解除したことを伝えるようにした
  - 色分け前にフィルタを有効化しようとした場合は、その場で戻して
    先に色分けが必要であることを伝える

## [1.3.1] - 2026-08-14

### 修正(不特定多数の利用を想定した堅牢性)
- **有効な数値が1つも無い属性を選ぶと、凡例に「Infinity 〜 -Infinity」と
  表示されていた問題を修正**。値が全て空欄の列などで発生する。
  着色を行わず、理由(値が空欄か数値として読めない)を伝えて止めるようにした
- **範囲の手動指定に数値として読めない値を入れると、黙って無視されていた**。
  「指定したのに効かない」と誤解されるため、どの入力を無視したかを通知する
- 空ファイルや短すぎるファイルを.glbとして読み込むと、DataViewの英語の
  内部エラーがそのまま画面に出ていたのを、意味の通るメッセージに変更

### テスト
- 壊れた入力とメッセージの検証を追加(36アサーション)。
  壊れたGLB・崩れたDXF・範囲が作れない数値・不正なIDを対象に、
  例外を出さないことに加えて**利用者に意味の通るメッセージが出ること**
  (内部の英語エラーが漏れないこと)を検証する

## [1.3.0] - 2026-08-14

### 読み込み
- **LWPOLYLINE形式のDXFに対応**。AutoCAD R14(1997年)以降の標準的なポリラインで、
  AutoCAD・QGIS・Illustratorなど一般的なCAD/GISツールが既定で書き出す形式。
  従来は POLYLINE / VERTEX のみ対応しており、これらのツールで作ったDXFは
  1件も読み込めなかった(利用者を増やすうえで最大の障壁だった)
  - 標高(コード38)をZ座標として全頂点に適用
  - 閉じフラグ(コード70)で選別し、断面線などの開いた線は輪郭の対象外にする。
    ただし閉じたものが1つも無い場合に限り開いたものを採用する
    (閉じフラグを立てないツールで「0個」の行き止まりにならないようにするため)
  - 円弧(バルジ)は直線として読み込み、件数を通知する
  - 除外・救済・直線化はいずれもステータスに明示する
- 輪郭が見つからない場合のメッセージを改善。対応形式と対処方法を具体的に示す
  (従来は「閉じたポリラインが見つかりませんでした」とだけ出るため、
  LWPOLYLINEのファイルでは原因を切り分けられなかった)

### テスト
- LWPOLYLINE形式の検証を追加(20アサーション)。頂点の組み立て・標高の適用・
  閉じフラグの選別・救済動作・既存形式の回帰を含む

## [1.2.2] - 2026-07-25

### 内部構造(挙動の変更なし)
- SVG書き出し2関数の重複(約150行)を解消。立面図SVGと現在のビューSVGは
  投影の求め方だけが異なりそれ以外は同一だったため、投影の違いを `view`
  オブジェクトに閉じ込め、`buildExportSVG()` / `readSVGExportOptions()` /
  `downloadSVG()` に共通化した。SVG機能の修正が1箇所で済むようになる
- `exportElevationSVG` 185行 → 91行、`exportCurrentViewSVG` 135行 → 68行。
  6行以上の重複は315行 → 131行
- 設定4パターン×2方式+平行投影の計9通りで、リファクタ前後のSVG出力が
  長さ・内容とも完全一致することを確認済み

## [1.2.1] - 2026-07-25

### テスト基盤(重大な欠陥の修正)
- **テストが古いコードを検証していた問題を修正**。`tests/extracted.js` の再生成が
  手動運用だったため、`normalizeId` を常に `"BROKEN"` を返すよう破壊しても
  94件全てが成功する状態だった。`run_all.js` の冒頭で `extract_for_tests.js` を
  必ず実行するようにした
- **test_fitview.js / test_winding.js がロジックの写しを検証していた問題を修正**。
  index.html をどう変更しても成功し続ける状態だった。実コードを検証するため、
  THREE・DOM に依存しない純粋部分を `solveFitDistance` / `solveFitOrtho` /
  `flipTriangleWinding` として index.html 側に切り出し、テストはこれを直接呼ぶ
- 平行投影のフィット(`solveFitOrtho`)と巻き順反転の検証を追加。
  アサーション数 94 → 111 件

### 修正
- モデルを読み込んでも「色分け済み」の状態が残り、未着色のモデルに対して
  実行ボタンが「色分けを再実行」のままになっていた問題を修正
  (`disposeModel` で状態を戻すようにした)

## [1.2.0] - 2026-07-25

### 表示・確認
- 視点プリセットに「上面」(真上から見下ろす)を追加
- 透視投影 ⇄ 平行投影の切り替えを追加。向き・ズームを保ったまま切り替わり、
  「現在のビューをSVGで保存」の書き出し結果にも連動する(平行投影時はパースなし)
- 軸ギズモをビュー右下に常時表示。読み込んだデータのX/Y/Z軸が現在の視点で
  どちらを向いているかが分かる
- 平行投影中はギズモ下にラベルを表示(既定の透視投影では非表示)

### UI(第三者視点レビューによる改善)
- **起動時に「色分けを実行」が最も目立つ問題を修正**: サンプルは起動時点で
  色分け済みなのに、唯一の塗りつぶしボタンが押下を促していた。実行後は
  設定変更が自動反映されボタンは不要になるため、輪郭線ボタンへ格下げし
  文言も「色分けを再実行」に変える
- **連番が「順番にやること」と誤読される問題を修正**: 実際には飛ばせる
  ステップ2・4・5・6の見出しに「(任意)」を明記。必須の流れは 1 → 3 → 7
- 初期展開を必須の1・3のみに変更(仕様書の記述と実装がずれていたものを修正)
- **タップ領域の拡大**: 開閉ヘッダー14〜15px・チェック行16pxと、iPad対応を
  掲げる割に小さすぎたため、いずれもWCAG 2.2の最小である24px以上を確保
- **色見本に文字ラベルを併記**: 何の色かをホバーのtitle属性でしか
  説明しておらず、ホバーの存在しないタッチ環境では判別不能だった
- 文字サイズの階層を整理(16/13/12/11pxの4段階)。アプリ名を16pxに拡大し、
  サブ見出しが配下のラベルより小さいという逆転を解消
- 開閉の見た目を統一(矢印を右端に揃え、記号・サイズ・色・回転を一致)
- ステータス表示を12px・本文色に強調し、日本語で字面が乱れる等幅指定をやめた
- ステップ1にサブ見出し(メインモデル/背景モデル)を追加
- 補足文を`.section-note`(セクション冒頭の説明)と`.hint`(直前のコントロールの補足)に
  書き分け、位置の意味を明確化

## [1.1.1] - 2026-07-24

### UI
- サイドパネル「3. 色分けする」「7. 書き出す」を用途ごとの小見出しで整理し、
  使用頻度の低い設定(範囲の手動指定・カテゴリ数調整・SVGの含める要素/線の太さ)を
  折りたたみ式の「詳細設定」にまとめて初期表示をすっきりさせた
- 「操作ガイド」リンクを説明文の行から独立させ、押しやすいサイズに変更
- ラベルの文言重複・不正確さを修正(「配色」見出しと同名だったフィールドラベルを
  「配色パターン」に変更。カスタムを含むため「プリセット」は不採用)

## [1.1.0] - 2026-07-24

### 書き出し
- 現在のビューをSVGで保存: 現在のカメラの位置・向き・ズームをそのまま使い、
  画面に見えている通りの遠近感(パースあり)でSVGを書き出す新モードを追加。
  立面図SVGと同じ凡例・ヒストグラム・ラベル・傾きラインの設定を共有するが、
  距離により見かけの縮尺が変わるためスケールバーは出力しない

## [1.0.0] - 2026-07-16

最初の正式リリース。

### 読み込み
- DXF(閉じた3Dポリライン)の読み込みと幾何属性の自動計算
- Rhino等が書き出す2Dポリライン形式のDXFにも対応(OCS座標の自動変換、
  同一レイヤー時のID自動一意化)
  (Width / Height / Area / FillRate / Tilt / RectAngle / Flatness / SelfIntersect / X / Y / Z / SrcID / Course)
- GLB(glTF 2.0バイナリ)+外部CSV属性の組み合わせに対応
- フォトグラメトリモデル(OBJ + MTL + テクスチャ)の背景重ね表示。
  複数ファイル同時選択またはフォルダごと選択。テクスチャは最大2048pxに自動縮小
- 原点シフトの共有(worldOffset)により、実座標系のままDXFと背景モデルが位置合わせされる
- 大規模データ対応: パース・属性計算のチャンク処理+プログレスバー
- iPad対応: タッチ操作(1本指回転・2本指パン・ピンチズーム)、ファイル選択、数値キーボード

### ID
- 読み込み時に立面図の左下から自動採番(下の段から、段内は左から右)
- 元IDをSrcID属性として保持(トレーサビリティ)
- 段(コース)番号をCourse属性として保存
- 手動の「IDを振り直す」ボタン

### 色分け・可視化
- 数値属性のグラデーション(赤→青 / 赤→緑 / 黄→青 / グレースケール / カスタム3色)、
  反転、範囲の手動指定、即時反映
- テキスト属性のカテゴリ色: 20色パレット+黄金角による自動生成(色数無制限)、
  出現数順の割り当て、「上位7カテゴリのみ色分け」オプション
- 凡例+ヒストグラム(24ビン、実際のグラデーション色で着色)
- 傾きライン: 外接矩形の水平軸に沿った中心線を矩形回転角の対称グラデーションで表示。
  色・太さ(20〜100mm実寸)のカスタマイズ、面のうねり(Flatness)に応じた浮かせ
- フィルタ: 任意の数値属性×範囲条件(複数・AND)、自己交差除外、半透明/非表示
- ラベル: 複数属性、文字サイズ、距離カリング
- 選択オブジェクトのハイライト(クリックでオレンジの輪郭強調)
- ダーク/ライトテーマ切替

### 書き出し
- CSV(BOM付きUTF-8、RFC 4180準拠のエスケープ)。データ名を設定すると
  全行にDataset列が付与され、複数データの横断分析(BIツール・pandas)で由来を識別できる
- ID付きGLB(法線の向きを壁全体で統一して出力)
- 立面図SVG: 平均法線による正射投影、表裏の自動判定、凡例・ヒストグラム・
  スケールバー・ラベル・傾きラインの埋め込み、要素の取捨選択、
  輪郭線の太さ指定(mm実寸)、塗りつぶしなし(線画)モード

### 操作性
- カーソル位置中心のホイールズーム(不動点の相似変換)
- 回転開始時のピボット自動補正(画面中央のオブジェクト表面へ)
- ダブルクリック/ダブルタップでフォーカス
- 視点プリセット(正面=立面図と同じ向き / 全体)
- 全体フィットのリセンタリング方式(余白5%、斜め視点でも均等)
- アコーディオンUI+ヘッダーへの設定状態サマリー表示

### 品質
- 幾何計算はNode.jsによる単体テスト(実コード抽出方式)と
  実データ統合テストで検証済み(tests/ 参照)
- リリース前の第三者視点レビューで以下を修正:
  - モデル再読み込み時にジオメトリ・マテリアルがGPUメモリに残り続けるリークを修正
    (disposeModelの導入。大規模データの読み直しを繰り返しても安定)
  - ファイル読み込み中に別の読み込みを開始でき、内部状態が破損しうる競合を修正
    (読み込みロックの導入。処理中は「完了までお待ちください」と案内)
  - 色計算関数にNaN/Infinityの防御を追加(通常経路では到達しない純関数レベルの保証)
