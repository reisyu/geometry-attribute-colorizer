# Geometry Attribute Colorizer — 開発ガイド

石垣調査（石垣BIM）ワークフローのための、ジオメトリ属性計算・色分け可視化ツール。
このファイルはClaude Codeがこのリポジトリで作業する際の前提とルールを記述する。

> 注: 前身のBlenderアドオン版（`CSV_Colorize_Addon`）は別リポジトリで凍結済み。
> 参照する必要はない。

## プロジェクトの性質

- **単一HTMLファイルで完結する構成**。`index.html` にJS/CSSがすべて含まれる
- 外部依存はCDNの Three.js (r128) と PapaParse (5.4.1) のみ。ビルド工程は無い
- すべての処理がブラウザ内で完結し、データを外部送信しない（設計上の重要な約束）
- GitHub Pagesで公開。`index.html` を差し替えれば即公開される
- 想定規模は最大1万オブジェクト

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体（唯一の実装ファイル。約7,000行） |
| `help.html` | 操作ガイド（アプリと同じデザインの独立した1枚） |
| `README.md` | 利用者向けの説明 |
| `SPECIFICATION.md` | 設計意図・判断の記録（なぜそうしたかを残す文書） |
| `CHANGELOG.md` | バージョンごとの変更履歴 |
| `LICENSE` | MIT License |
| `sample_ishigaki.dxf` | 同梱サンプルデータ（内蔵サンプルの元データでもある） |
| `tools/embed_sample.js` | サンプルDXFから内蔵サンプル（座標のみ）を再生成する |
| `tools/build_offline.js` | オフライン配布用の単一HTMLを作る（ライブラリを埋め込む） |
| `extract_for_tests.js` | index.html から実関数を抽出して tests/extracted.js を作る |
| `quickstart.html` | 配布・印刷用の1枚手順書（A4縦。dist にも入る） |
| `build_offline.bat` | 上記をダブルクリックで実行する入口（Windows用。ASCIIのみで書くこと） |
| `assets/hero-elevation.svg` | READMEの見出し画像。アプリ自身のSVG出力を使う |
| `tests/` | Node.jsで動くテスト一式 |

## 開発時の必須ルール

### 1. 計算ロジックを変更したらテストを実行する

テストは `index.html` から実関数を抽出した `tests/extracted.js` に対して実行する。
**`run_all.js` が冒頭で抽出を自動実行する**ので、通常はこれだけでよい。

```bash
node tests/run_all.js       # 抽出 + 全テストを一括実行（16ファイル・313件）
```

抽出の自動化は必ず維持すること。以前は手動だったため、`index.html` を壊しても
古い `extracted.js` に対してテストが全て成功してしまう状態だった
（`normalizeId` が常に "BROKEN" を返すよう改変しても全件成功した）。

個別に実行する場合（いずれも `tests/` 内）:

| ファイル | 内容 | 件数 |
|---|---|---|
| `test_version.js` | バージョン表記の一致（index/help/仕様書/CHANGELOG） | 8 |
| `test_suite.js` | 幾何計算・色計算 | 64 |
| `test_rectline.js` | 傾きラインの計算 | 11 |
| `test_palette.js` | カテゴリ色パレット | 3 |
| `test_fitview.js` | 全体表示のフィット計算 | 22 |
| `test_winding.js` | GLB法線の巻き順 | 7 |
| `test_adversarial.js` | 異常入力への耐性 | 12 |
| `test_badinput.js` | 壊れた入力とエラーメッセージ | 36 |
| `test_rhino_dxf.js` | Rhino形式DXFの読み込み | 9 |
| `test_lwpolyline.js` | LWPOLYLINE形式DXFの読み込み | 20 |
| `test_aspect.js` | アスペクト比 | 16 |
| `test_zip.js` | ZIP書き出し(CRC32・ZIP構造) | 19 |
| `test_thickline.js` | 選択の輪郭線(太線の板の組み立て) | 23 |
| `test_selection.js` | ブラシ選択の当たり判定 | 15 |
| `test_classify.js` | 分類の列(正規化・予約列名) | 43 |
| `integration_test.js` | 実データでの統合テスト | 5 |

**全テストが通ることを確認してからコミットする。**

### 2. UI・描画の変更はブラウザでの手動確認が必要

テストは計算ロジックしかカバーしていない。過去に「計算は正しいが、
メッシュへの保存時にキーを1つ入れ忘れて3D表示だけ動かない」バグが発生している
（傾きラインの `rectLine.n` の入れ忘れ）。
UI・3D描画・SVG出力を変更したら、必ずブラウザで実際に動かして確認すること。

### 3. バージョン管理

- セマンティックバージョニング（メジャー.マイナー.パッチ）
- バージョンは `index.html` の `APP_VERSION` 定数と `help.html` の
  `HELP_VERSION` 定数の**両方**を更新する
- バージョンは `SPECIFICATION.md` 冒頭の「対象バージョン」も揃える
  （`test_version.js` が4箇所の一致を検査する）
- リリース時は `CHANGELOG.md` に追記し、`git tag -a vX.Y.Z` を打つ
- **pushはブランチとタグを分けて送る**。同時に送るとGitHub Pagesのビルドが
  起動しない（§12.2・§12.4に実測記録あり）

  ```bash
  git push origin main
  git push origin vX.Y.Z
  ```

## 設計上の重要な決定（変更しないこと）

- **RectAngleの正方形問題には対応しない**。Width≒Heightのオブジェクトで角度が
  ±45°付近で不安定になる件。実データで実害がないため対応不要と判断済み
- **座標の原点シフト（worldOffset）は必須**。実測座標（平面直角座標など）を
  そのまま扱うとfloat32精度が破綻するため、読み込み時に原点付近へ寄せている
- **傾きラインは面から Flatness 分だけ浮かせる**。オブジェクトは完全な平面ではないため、
  面上に置くと凹凸に埋まって線が途切れる
- **法線の向きは「壁全体の平均法線」で統一する**。個々の輪郭の巻き方向はバラバラなので、
  平均法線を基準に揃える（傾きライン・SVG立面図・GLB書き出しで共通の考え方）
- **色分けの範囲は既定で手動指定（オン）**。誰が開いても同じ配色になり、
  書き出したSVGを並べて比較できるようにするため。利便性を理由に既定を
  自動へ戻さないこと
- **利用者の指定した設定を黙って変えない**。範囲が成立しない場合も自動で
  切り替えず、確認ダイアログで選ばせる（`showRangeDialog`）
- **全体フィットはリセンタリング方式**。距離の二分探索＋画面内平行移動で余白を
  均等配分する。単純な対称フィットでは斜め視点で奥側に余白が残るため
- **描画は2パス**（背景 → 深度クリア → 調査対象）。輪郭モデルは平らな板なので
  フォトグラメトリの凸凹した表面と場所を奪い合い、色がまだらになる。深度の微調整
  （`polygonOffset`）では直らない食い込みなので、パスを分けて常に手前に描く（§6.2.1）。
  背景色は `bgScene` が持つ。`scene` にも持たせると背景パスを塗り潰してしまう
- **不透明度を決める場所は1つ**（`applyMeshOpacity`）。フィルタの除外表示と
  背景を透かす表示の2つの理由があり、別々に書くと片方が他方を1.0に戻す
- **WebGLは線の太さの指定を無視する**（常に1px）。太い線が要るときは板を張る。
  傾きラインは実寸mm、選択の強調は画面px（§6.5.1）
- **設定は「対象の隣」に置く**。対象を持たない設定を既存のステップに間借りさせない。
  傾きラインを使わない利用者には見つからない場所に隠れる（§10.7）。
  実際にv1.21.0で選択の輪郭線の太さをステップ4に置いて指摘を受けた
- **手で選ぶ必要があるものは、属性では代用できない**。孕み出しの範囲のような
  「計算で表せないまとまり」だから手で選ぶ。フィルタや段での代用案は成り立たない
  （計算で表せる集合ならメモを書く必要がない、というメモと同じ構造。§6.8）
- **モードで奪うのは回転だけ**。パン・ズームは選択モードでも通常モードと同じにする。
  選択は分単位の作業で、途中で見る場所を変えられないと使えない（§6.8）
- **手入力の列を増やしたら、失わせない仕組みも必ず一緒に広げる**。未保存の表示と
  ブラウザへの控えは「メモだけ」ではなく「手で書いた列すべて」が対象（§5.1.3）
- **開いた語彙は「ゆれを防ぐ」より「ゆれを直せる」で設計する**。正規化は空白と
  全角英数まで。漢字の異体字は機械では判断できないので触らず、後から統合できるようにする

詳しい設計意図は `SPECIFICATION.md` を参照。**設計判断を変更する際は必ずそこも更新する。**

## 用語の統一

- UI表示・ヘルプ本文の総称は「**オブジェクト**」を使う（汎用ツールとしての立ち位置）
- 「石」「石垣」は、石垣調査という具体的文脈を説明する箇所でのみ使う
- 傾きライン（旧称: 通り線 / 回転角ライン）— 現在の正式名称は「傾きライン」

## コーディングスタイル

- コメント・UI文言はすべて日本語
- コメントは**「何をしているか」ではなく「なぜそうするか」を書く**
- 既存のスタイル（インデント2、セミコロンあり）に合わせる
- CSS変数（`--accent` など）を使い、ダーク/ライト両テーマで破綻しないようにする
- 新しい外部ライブラリは原則追加しない（単一ファイル・低依存の方針）
- ステータス表示は左アクセントバーの「通知」、ボタンは面色ありの「操作」と
  視覚的な役割を分けている。この区別を崩さない

## 現在の状況

- **v1.23.4 を公開中**（https://reisyu.github.io/geometry-attribute-colorizer/）
- GitHub Releaseは v1.16.0 まで作成済み（v1.13.0〜v1.16.0は1本にまとめた）。
  作成は作者が手動で行う（`gh` CLI・トークンは未設定なので、Claude Codeからは
  作成できない。文面を用意して作者に渡す。タイトルと本文は別ファイルにする）
- **タグはクラウド上のセッションからはpushできない**（403。§12.2に実測記録）。
  タグ打ちはPCで行う。その場で打てなくても後日まとめて付ければよい
- タグは**バージョンを上げたコミット**に付ける（§12.1）。同じ版のまま
  ドキュメントだけ直すコミットが続くため、どちらに付けるか毎回迷う
- 直近で追加した主な機能
  - メモ欄 — オブジェクトごとの所見。CSVのNote列（§5.1.2）
  - メモの有無での色分け（HasNote。あり=赤・なし=グレーに固定）とラベル表示
  - メモのブラウザ自動保存と復元、未保存の3箇所表示（§5.1.2）
  - CSV読み戻しを元ID(SrcID)で照合（§6.3.2）
  - 自己交差を0〜0.5の実数に（交差の大きさ。§5.1.1）
  - まとめて書き出し(ZIP) — 属性ごとの立面図を1つのZIPに（§8.7）
  - 背景モデルとの前後関係を2パス描画で統一（§6.2.1）
  - 選択の輪郭線を太く（3px固定。画面px指定。§6.5.1）
  - まとめて選ぶ（ブラシ）とまとめてメモ（§6.8）
  - 分類の列（石種・エリアなどの手入力。§5.1.3）

### 検討中の項目

- **メモの自動保存とセッション復帰** — 現状ページを再読み込みすると消える。
  現場での中断・バッテリー切れで失われる（§5.1.2の割り切り）
- 異常値の検出とフィルタリング（当初の検討リストで唯一の未着手。
  自己交差の連続値化で一部前進した）
- `parseDXF` の重複頂点除去 — 閉じ頂点が重複したDXFで長さ0の辺ができる。
  SelfIntersectは解消したが、Newell法線・靴ひも面積・凸包には渡り続けている

### 過去に不具合を出した箇所（変更時は特に注意）

- **表示と実際の値が食い違う類の不具合**を繰り返し出している。
  画面上は正しく見えるため利用者が気づけず、影響が大きい
  - フィルタ条件の属性が黙って別の属性に付け替わる（§6.3の「属性が無くなった条件の扱い」）
  - CSV差し替え後も古い紐づけで絞り込む（§6.3.1）
  - 色をリセットしても凡例の状態が残り、図と説明が食い違う（§8.6.1）
  - CSV読み戻し時にIDで突き合わせ、DXFを作り直すと別の石に値が付く
    （v1.20.0で元ID照合に修正。§6.3.2）
  - メモを書いても属性表の「メモの有無」が更新されない（v1.16.1で修正）。
    表は石を選んだ瞬間にしか作られておらず、裏の値は正しいのに
    画面の文字だけが取り残されていた
  - CSVを読み込み直した後、行の対応が古いまま残り、そこに書いたメモが
    どこにも残らない（§5.1.2「行の対応は、CSVを読み込んだ時点で作り直す」）
  - フィルタで非表示にしたものがクリックで選べる。Three.js r128のレイキャストは
    visible=false を無視する（実測。v1.22.1で修正）
  - 押した瞬間と離した瞬間で同じオブジェクトを足して外す自己衝突（§6.8）。
    ポインタ操作を足すときは「押す・動く・離す」の3点が打ち消し合わないか必ず確かめる
  - カテゴリの色を出現数順に割り当てていたため、値を1つ足すと既存の色が
    入れ替わった（v1.23.2で修正。§6.1）。**画面の状態は、利用者が触っていない
    ものを勝手に変えてはいけない**
  - 画面の凡例にだけ「未入力」を足し、SVGの凡例に足し忘れていた（v1.23.3で修正）。
    凡例を変えたら**画面とSVGの両方**を直す（§8.6.1と同じ型）
  - 未保存の件数を3箇所で別々に数えており、畳んだステップ7の見出しだけが
    メモしか数えていなかった（v1.23.4で修正）。**同じ数を出す場所は
    同じ関数から取る**（`unsavedManualSummary`）
- **メモに関わる変更は「失われないか」を必ず確かめる**。メモだけは再計算できない。
  デバウンス中の離脱、控えを取れないデータ、行の取り違え——いずれも画面上は
  正常に見えるまま失われる。v1.20.1で3件まとめて塞いだ（§5.1.2）
- **ファイル名に使う文字は必ず `safeFileNamePart` を通す**。
  CSVの列名をそのまま使うと、ZIPの中でフォルダになったり展開先の外に出たりする（§8.7.1）
