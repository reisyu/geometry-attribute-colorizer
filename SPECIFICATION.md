# Geometry Attribute Colorizer 仕様書

作成日: 2026-07-09 / 最終更新: 2026-08-30
対象バージョン: v1.16.0(APP_VERSION定数がコード内に埋め込まれ、画面とSVG出力に表示される)

この文書は、機能の仕様に加えて「なぜその設計にしたか」という意図を記録するためのものです。
今後の機能追加・変更の際は、ここに書かれた意図と矛盾しないかを判断基準にしてください。

---

## 1. 目的と背景

### 1.1 解決したい課題

石垣(伝統的石積み)の調査ワークフローにおいて、各石の幾何属性(幅・高さ・面積など)を
計算し、属性に応じて色分け可視化する工程は、従来Grasshopper(有償のRhinoceros上で動作)で
行われていた。有償ソフトが前提であることが、調査関係者への普及の障壁になっていた。

**本アプリの目的: この属性計算・可視化の工程を、無償・インストール不要のブラウザ
アプリとして提供し、より多くの人が使えるようにすること。**

### 1.2 想定ワークフロー全体における位置づけ

1. 石垣をフォトグラメトリで3Dモデル化(OBJ + MTL + テクスチャPNG) — 外部ソフト
2. クラスタリングソフトで各石の輪郭を抽出(DXF、閉じた3Dポリライン) — 外部ソフト
3. **本アプリ: DXFを読み込み、属性を自動計算し、色分け表示・図版出力・データ書き出し**
4. 必要に応じてCSV/GLBで書き出し、他ツール(Blender等)で分析・再利用

ステップ1・2のWebアプリ化は検討の結果見送った(フォトグラメトリは計算量的に非現実的、
輪郭抽出は既存ソフトの品質再現コストが高い)。ただしステップ1の成果物(OBJ)を
「背景モデル」として重ね表示する機能は搭載した。

### 1.3 開発経緯(意図の変遷)

- 当初は「GLB + 外部CSVを読み込んで色分けする」ビューアーとして出発
  (アプリ名も当初は CSV Attribute Colorizer だった)
- その後、Grasshopperで行っていた属性計算そのもの(平面化・最小外接矩形・
  幅高さ判定・充填率)をアプリ内に実装。**CSVを外部で用意する必要がなくなり、
  DXFを放り込むだけで完結する形へ発展した**
- これに伴い、実態に合わせてアプリ名を Geometry Attribute Colorizer に変更した
- 同機能のBlenderアドオン版も並行開発したが、Webアプリが大きく先行したため
  アドオン版は凍結。Blenderが必要な場面は「ID付きGLB書き出し→Blenderで読む」で
  接続する方針とした

---

## 2. 設計原則

以下は個別機能に優先する全体方針である。

| 原則 | 内容と意図 |
|---|---|
| **単一HTMLファイル** | index.html 1つで完結。配布・GitHub Pages公開・オフライン利用が容易。外部依存はCDNのThree.jsとPapaParseのみ。GLB/OBJ/DXFのパーサーは全て自前実装(CDNに存在しないローダーへの依存で動かなくなった教訓による) |
| **ブラウザ内完結** | 読み込んだファイルは一切サーバーへ送信しない。調査データは公開前の機密であることが多く、プライバシーはツールの信頼性そのもの |
| **iPad対応** | 現場での利用を想定。accept属性を使わない(iOSでグレーアウトする)、タッチ操作(ピンチズーム・2本指パン)、inputmode="decimal"、アコーディオンUIで縦長パネルを圧縮 |
| **大規模データ耐性** | 実データは2,500〜10,000オブジェクト規模。重い処理はチャンク分割(200個単位)+プログレスバーでUIを固めない |
| **用語は「オブジェクト」で統一** | 石垣特化ではなく汎用ツールとしての表現を採る。「石」「輪郭」はサンプルの説明など内容記述に限る |
| **検証してから出す** | 幾何計算は既知の入力での検証(Pythonでの再現テスト)を経てから組み込む |

---

## 3. 座標系と原点シフト(最重要の設計判断)

### 3.1 前提

- 入力データ(DXF/OBJ)は **Z軸が鉛直(上)、XY平面が水平** のZ-up座標系
- 座標値は平面直角座標系などの実座標(例: X≈-43,000、Y≈-145,000)であり、
  原点から数万〜数十万単位離れている

### 3.2 問題と対策

GPU(WebGL)の頂点座標はfloat32(有効約7桁)であり、大きな座標をそのまま使うと
精度が崩壊する(頂点のガタつき、Zファイティング)。そのため:

- **読み込み時に全体のバウンディングボックス中心を原点へシフト**する
- シフト量は `worldOffset` というグローバルな共有値として保持する
- **先に読み込んだデータ(通常DXF)がシフト量を決め、後から読むデータ(OBJ背景)は
  同じ値を再利用する**。これにより実座標系のまま両者がぴったり重なる
  → 運用上「DXFを先に読み込む」ことを推奨(READMEにも明記)
- Three.js内部はY-upのため、`(x, y, z) → (x-ox, z-oz, -(y-oy))` で変換する
- 属性のXYZ座標は**シフト前の元の値**で出力する(GIS等との照合のため)

---

## 4. 入力データ仕様

### 4.1 DXF(主入力)

- 対応形式: **LWPOLYLINE** と **POLYLINE / VERTEX** の2エンティティ。
  - **LWPOLYLINE**(AutoCAD R14以降の標準形式)。AutoCAD・QGIS・Illustrator等、
    一般的なCAD/GISツールが既定で書き出すのはこちら。
    頂点を10/20の並びで直接持ち、Zは**標高(38)が全頂点に共通**で効く。
    **SEQENDを持たず、次の要素の開始(コード0)かファイル末尾で終わる**
  - **POLYLINE / VERTEX**: 閉じた3Dポリライン(フラグ70=9。クラスタリングソフトの出力形式)、
    および2Dポリライン(Rhino等の出力形式)
  - どちらも押し出し方向(コード210/220/230)で定義されるOCS座標を持ちうるため、
    **Arbitrary Axis AlgorithmでWCSへ自動変換**する
  - POLYLINEヘッダ自体の10/20/30は頂点として拾わない(VERTEX内のみ収集)
  - レイヤー名に数字がない/全ポリラインが同一レイヤーの場合は、IDに連番を付けて
    一意化する(その後の自動採番で振り直されるため実害なし)
- 1ポリライン = 1オブジェクト(石)
- **レイヤー名の末尾の数字が読み込み時のIDになる**(例: `Layer123` → `123`)
- 輪郭は完全な平面でなくてよい(実データは平面から平均28mm・最大108mm程度ズレる。
  最適フィット平面に投影して計算する)
- パーサーは自前実装。LINE・SPLINE・CIRCLE等は非対応

### 4.1.1 LWPOLYLINE対応の設計判断

**背景**: 当初はクラスタリングソフトの出力(POLYLINE)とRhinoの2Dポリラインのみに
対応し、「開発元にフォーマットを変えないよう依頼する」方針だった。
しかしLWPOLYLINEは1997年以降のCAD標準であり、**一般的なツールで作った
DXFが1件も読めない**状態は、利用者を増やすうえで最大の障壁だった。

- **閉じフラグ(70のビット1)で選別する**: 一般的なCADのDXFは断面線・補助線など
  開いた線を含むことがあり、それらを輪郭として扱うと無意味な属性が生まれる。
  そのため既定では閉じたものだけを採用する
- **ただし閉じたものが1つも無い場合に限り、開いたものを採用する**:
  閉じフラグを立てないツールも存在し、そこで「0個」の行き止まりになると
  利用者は原因を切り分けられないため。救済したことは読み込み時に通知する
- POLYLINE側は閉じフラグを見ない(既存挙動を維持)。クラスタリングソフトの
  出力は常に閉じており、選別する必要がないため
- **円弧(バルジ、コード42)は直線として扱う**。形状を変えて読み込む以上、
  黙って進めず件数を通知する
- 除外・救済・直線化はいずれも**必ずステータスに出す**。
  形状やデータ件数が変わる処理を無言で行わないことを原則とする

### 4.2 GLB(glTF 2.0 バイナリ)

- 自前パーサーで読み込み(ヘッダー/JSONチャンク/BINチャンク解析)
- ノード名がオブジェクト名(=CSVマッチング用ID)になる
- .gltf+.bin分離形式は非対応(エラーメッセージで案内)
- GLBモデルは三角形メッシュのため、属性の自動計算・SVG書き出しの対象外。
  外部CSVとの組み合わせで色分けする用途

### 4.3 CSV(外部属性)

- 1行目ヘッダー、ID列 + 任意の属性列。数値/テキストは自動判定
- ID正規化: `"1"`, `"1.0"`, `" 1 "` の表記ゆれを吸収して照合する
- 欠損値(空欄)は安全にスキップ(実データに欠損があった経験から)

### 4.4 OBJ + MTL + テクスチャ(背景モデル)

- フォトグラメトリモデルの重ね表示専用。属性計算・クリック判定・書き出しの対象外
- ブラウザの制約上、OBJ選択時に隣のMTL/PNGを自動で読むことは不可能。
  **「複数ファイル同時選択」または「フォルダごと選択(webkitdirectory)」**で対応
- **テクスチャは読み込み時に最大2048pxへ自動縮小**(意図: 実データは25MB PNG×15枚で、
  原寸ではGPUメモリを数GB消費し破綻するため。背景用途では2048pxで十分)
- マテリアルはMeshBasicMaterial(意図: フォトグラメトリのテクスチャは陰影焼き込み済み
  なので照明計算は不要。軽く、実物の見た目に忠実)
- 描画は両面(DoubleSide)。輪郭表示も同様(意図: Rhino系データは法線の向きが
  まちまちで、片面描画だと面が消える)

---

## 5. 自動計算される属性

DXF読み込み時、各オブジェクトについて以下を計算しCSV化する。
列名は英語(他ツールとの互換性)、UI表示は「Width(幅)」のように日本語併記。

| 列名 | 日本語名 | 定義 | 実装上の要点 |
|---|---|---|---|
| Width | 幅 | 最小外接矩形の2辺のうち水平に近い方 | 矩形の2軸を3Dに戻し、Z成分(鉛直成分)が小さい方の軸を幅とする |
| Height | 高さ | もう一方の辺 | 同上 |
| Area | 面積 | フィット平面に投影した輪郭の面積 | 靴ひも公式 |
| FillRate | 矩形充填率 | Area ÷ 外接矩形の面積 | 0〜1。1に近いほど矩形に近い形。全データで0〜1に収まることが計算の正しさの検証にもなる |
| Aspect | アスペクト比 | Width ÷ Height | 1より大きい=横長、小さい=縦長。**形状固有の値ではなく向きに依存する**(WidthとHeightが水平/鉛直で決まるため、90度回すと逆数になる)。高さが潰れた輪郭ではnull(空欄)にしてInfinityを作らない |
| Tilt | 傾斜角 | 面の法線と鉛直(+Z)のなす角(度) | 0°=水平面、90°=垂直面。法線の符号に依存しないよう0〜90°に正規化 |
| RectAngle | 矩形回転角 | 外接矩形の水平辺が「面内の水平基準」からなす角(度) | 面内水平基準=法線×鉛直(走向方向)。反時計回り(右上がり)が正、±45°の範囲。**面内で測る**(3D空間の絶対角ではない)ことがユーザー要件 |
| Flatness | 平面性 | フィット平面からの最大ズレ距離 | 大きいほど平面近似の信頼性が低い。品質フィルタ用 |
| SelfIntersect | 自己交差 | 輪郭の自己交差の大きさ(0〜0.5、0=交差なし) | 交差で分かれる小さい側のループが全体に占める面積比。**Areaはこの値の2倍だけ過小評価される** |
| X, Y, Z | 重心座標 | 輪郭重心の**シフト前**の座標 | GIS照合用。Zで色分けすると標高グラデーションになる |
| SrcID | 元ID | 読み込み時のレイヤー名由来ID | 自動採番(§7)で振り直される前の値。クラスタリングソフト側との照合用トレーサビリティ |

### 5.1 計算アルゴリズムと選定理由

1. **法線: Newell法** — 閉じたほぼ平面のポリゴンに対して安定。PCAより軽量で、
   輪郭(順序付き頂点列)に適する
2. **平面化** — 重心を通るフィット平面に頂点を投影して2D化
3. **最小外接矩形: 回転キャリパー法** — 「最小矩形の一辺は凸包のいずれかの辺と
   重なる」という定理により、凸包の辺の数だけ試せば**厳密解**が得られる。
   対象が「完全に平面」というユーザー確認を得て、3D版(O'Rourke法)ではなく
   2D版で十分と判断した
4. **凸包: Andrew's monotone chain** — O(n log n)、実装が簡潔

### 5.1.1 自己交差の判定(SelfIntersect)

フィット平面に投影した2D輪郭で、**頂点を共有しない辺のペア**の線分交差を総当たりで
調べる。値は0/1の二値ではなく、**交差の大きさを表す0〜0.5の実数**である。

#### なぜ二値ではないのか

DXFの輪郭は、OBJのテクスチャ画像上でクラスタリングした結果をメッシュに投影して
作られている。石垣のように凹凸のある面では、これは平面ではない3Dポリラインになる。
そのため「投影の結果として生じる小さな交差」と「頂点の順序が壊れた輪郭」を
区別する必要があり、二値では表現できない。

ただし**当初考えていた理由は誤りだった**。「凹凸があるから投影で交差が出る」は
成り立たない。実測: 内蔵サンプルの平面外成分を20倍にして平均Flatnessを37mm→733mmに
しても、**投影後の2D座標は0.00mmしか動かず、交差は1件も出なかった**。
フィット平面への正射投影は法線方向の成分をちょうど捨てる線形写像であり、
点が法線方向にどれだけ離れていても投影像は変わらないためである。

投影由来の交差が実際に生じるのは、**輪郭が視線方向に対して折り返しているとき**
(メッシュのオーバーハングを回り込んだ輪郭など)、つまりフィット平面が
その石を代表できていないときである。投影方向を法線からθ傾けて測ると:

| θ | 交差した輪郭 | 面積比(中央値 / 90% / 最大) |
|---|---|---|
| 0° | 0 / 488 | — |
| 20° | 4 / 488 | 0.04% / 0.33% / 0.33% |
| 30° | 7 / 488 | 0.03% / 2.72% / 2.72% |
| 45° | 10 / 488 | 0.12% / 2.71% / 2.71% |
| 75° | 70 / 488 | 0.76% / 8.77% / 15.76% |

一方、頂点の順序が壊れた輪郭(頂点2つを入れ替えて生成した4,388件)の面積比は
**中央値12.6%・最大50%**。現実的な範囲(θ≤45°)の投影由来は3%以下に収まり、
両者は桁で分かれる。**この分離が、連続値にする根拠である。**

#### 値の定義

交差点で輪郭はちょうど2つのループに分かれる。その**小さい側の面積が、その交差に
よって生じた誤差の実体**であり、全体に対する比を値とする。上限は0.5(2つのループが
同面積のとき)。

    値 = min(ループA, ループB) / (ループA + ループB)

#### しきい値は「誤りかノイズか」ではなく「属性がどれだけ狂うか」で決める

面積比を r とすると、**Areaは 2r だけ過小評価される**。自己交差した輪郭では
小ループが逆向きに周回するため、靴ひも面積が `|大 − 小|` になる(本来は `大 + 小`)。
交差1個のケース2,018件すべてで係数がぴったり2.0000であることを確認した。

| 面積比 | Areaの過小評価 |
|---|---|
| 0.5% | 約1% |
| **1%** | **約2%** |
| 5% | 約10% |

これによりしきい値に物理的な意味が与えられる。おすすめフィルタの `0.01` は
「Areaを約2%以上狂わせる交差だけを除く」という意味であり、恣意的な%ではない。
**この根拠を失う形でしきい値を変えないこと。**

#### 判定式は三値の向き判定にする

向きの判定は `orient()` で -1/0/+1 の三値を返す。外積を2辺の長さで正規化した
相対値(角度の正弦)が `1e-12` 未満なら共線とみなす。座標は原点シフト後のメートル値で
輪郭は0.1〜1.5m規模のため、絶対値のしきい値ではスケールに依存する。

二値(`cr > 0`)で書いてはならない。共有頂点や長さ0の辺で外積が厳密に0になったとき
「負」へ倒れ、正常な輪郭を交差と誤判定するためである。

#### かつてのリング距離Kによる除外(廃止済み)

以前は輪郭を一周する経路上での辺の隔たり(リング距離)が K 以下のペアを判定から
除外していた(`K = max(1, min(3, floor(n/6)))`)。これは上記の二値判定の退化を
**距離で迂回するための当て木**であり、次の実害があった。

- 除外しないと内蔵サンプル123輪郭のうち111件(90%)が「交差あり」になっていた。
  その111件はすべて最後の辺と最初の辺の組で、共有頂点P0で外積が0になり
  判定が退化することが原因(微小な交差ではない)
- K≥2 は実データ上の根拠が無く、**離れた辺どうしの本物の交差を捨てていた**。
  変異データでの実測で、12頂点以上の輪郭では交差の14〜16%を、
  全体では4.9%を見逃していた
- 閉じたポリラインの終端に始点を書き出すDXF(長さ0の辺ができる)では、
  **全オブジェクトが「自己交差あり」になっていた**

三値の向き判定に変えたことで、これらはいずれも除外の工夫なしに解消した。
**Kのような距離による除外を再導入しないこと。**

#### 実装上の注意

- **AABBによる枝刈りを外さない**。三値判定は `hypot` を使うぶん重く、
  枝刈りが無いと1万輪郭で12ms→80msになる。枝刈り込みで現状と同等(12ms)
- **交差が複数ある輪郭では、ループ分割は厳密でなくなる**(変異データでは
  4,388件中2,370件が交差2個以上)。各ループ自体がさらに交差しており、
  靴ひも面積では逆向きに周回する部分が相殺されるためである。
  **ずれは両方向に出る**(当初「過小評価側=安全側に倒れる」と書いていたが、
  実測の結果これは誤りだった)。真の値を
  `|巻き数≠0の領域の面積 − 靴ひも面積| ÷ (2 × 巻き数≠0の領域の面積)`
  として比べると:

  | 交差数 | 件数 | 実装値(中央) | 真値(中央) | 実装/真値(中央) | 過大評価の割合 |
  |---|---|---|---|---|---|
  | 1 | 2,002 | 0.037 | 0.037 | 1.000 | (厳密に一致) |
  | 2 | 2,014 | 0.222 | 0.205 | 0.96 | 30% |
  | 3 | 134 | 0.110 | 0.065 | 1.02 | 51% |
  | 4以上 | 222 | 0.194 | 0.113 | 1.20 | 72% |

  **交差1個のときは厳密**で、複数のときだけ近似になる。それでも許容するのは、
  おすすめフィルタのしきい値0.01をまたいで判断が変わるのが交差2個以上の
  **2.8%(66/2370)**にとどまるためである。交差が複数ある輪郭は真の値の中央値が
  0.20と、いずれにせよしきい値から遠く離れている。
  厳密にするには全交差点で輪郭を分割してアレンジメント(平面分割)を組み、
  各面の巻き数を求める必要があり、100行規模の実装になる。
  **しきい値を0.05以上へ動かす場合は再検討が必要**(0.05では不一致が4.6%に増える)
- 近接した辺だけで作られる小さな自己交差は、原理的に検出できない
- 判定は投影後の2Dで行うため、3Dではねじれているだけの輪郭が投影の結果として
  交差しうる。それこそがこの値で測りたいものだが、Flatnessと併せて見るべき値である

#### 検証の限界

上記の測定のうち、投影由来の分布は**投影方向を傾けて作った合成データ**、
誤り側は**頂点入れ替えによる合成データ**である。内蔵サンプルには真の自己交差が
1件も無いため、実際の欠陥輪郭で測ったものではない。
大規模な実データで面積比のヒストグラムを取れば、しきい値の妥当性を確認できる。

### 5.1.2 メモ(Note列) — 唯一、再計算できない属性

§5の属性はすべてDXFから何度でも作り直せる。**メモだけは違う**。人が現場で見て
書いたものであり、失えば戻らない。この非対称性が、以下の設計をすべて決めている。

- **列は最初に書き込まれたときに作る**。使っていないデータのCSVに空のNote列を
  増やさない(データ名からDataset列を作るのと同じ考え方、§8.6)
- **色分けとまとめ書き出しの対象から外す**(`visualizableColumns`)。
  自由記述は値がほぼ全件異なるため、カテゴリ色が石の数だけ生成されて
  意味のない虹になる。
  フィルタは数値列限定(`numericFilterColumns`)なので、こちらは何もしなくても外れる
- **代わりに「メモの有無」(`HasNote`)を自動で作る**。値は「あり」「なし」の2つだけなので、
  カテゴリ色2色で塗り分けられ、記入済みの箇所が図で一望できる。立面図にも出せる。
  「本文は色分けに向かないが、有無は向く」という性質を、派生列として切り出したもの
  - **全行に必ず値を入れる**。空欄のままにすると §4.3 の欠損値スキップでその石だけ
    色が付かず、「なし」なのか「未計算」なのか見分けられなくなる
  - **メモを一度も書いていないデータには作らない**。使わない列をCSVに増やさない
  - 書き戻したCSVを読み込んだときも作り直す(`loadCSVText`)。往復しても
    有無での色分けがそのまま使える
- **ラベルには本文を出せる**が、`labelText`で切り詰める。メモは複数行の長文になりうるので、
  改行を空白に潰して先頭12文字だけ見せる。全文はインスペクタで読む前提。
  3Dラベルと立面図SVGの両方が同じ関数を通る(片方だけ長文が出る食い違いを作らない)
- **`<textarea>` を使う**。複数行が書けるだけでなく、iOS/Androidの標準キーボードの
  **音声入力**が使える。現場では打つより速く、専用の実装を持たずに手に入る
- **CSVのエスケープは既存資産で足りる**。メモは改行・カンマ・引用符を含みうるが、
  §8.6のRFC 4180準拠のエスケープがそのまま効くため追加実装は無い

#### 失わせないための3点

保存の仕組みを持たない(サーバが無い)以上、**書き出しが唯一の保存手段**である。
利用者がそれを知らないまま失うことがないよう、3箇所で主張する。

1. メモを書くと「メモが未保存です」を常時表示する(CSVで保存すると消える)
2. **ページを閉じようとしたら引き止める**(`beforeunload`)
3. **ファイルを読み込む前に確認する**(`beginLoading`)。読み込むと属性テーブルが
   作り直されてメモは消えるため、黙って捨てない

#### 現時点の割り切り

- **1オブジェクトにつき1つ**。点検記録として追記していく使い方(1対多)は、
  長形式の別テーブルと派生列(メモ件数・最終点検日)が要る。構造が変わるため別途
- **復元はCSVの往復のみ**。ページを再読み込みすると消える。自動保存は持たない
- **ID照合の問題を先送りしている**。書き出したCSVを読み戻すときIDで突き合わせるが、
  IDは§7で自動採番されるため、DXFを作り直すとずれる。年をまたぐ運用にするなら
  SrcIDでの照合が必要になる

### 5.2 既知の限界(意図的に保留したもの)

- **RectAngleの正方形問題**: Width≒Heightのオブジェクトは水平辺の判定が僅差で
  反転し、値が±45°付近で不安定になる。検討の結果、**対応不要と決定済み**
  (実データでは正方形に近い石が少なく、実害がないため。今後も対応しない)
- (解消済み)テキスト属性のカテゴリ色は当初8色の使い回しだったが、v1.0.0で
  20色パレット+黄金角生成に刷新した(§6.1参照)

---

## 6. 色分け・可視化

### 6.1 色分け

- 数値属性: グラデーション(赤→青 / 赤→緑 / 黄→青 / グレースケール / カスタム)。
  既定の向きは「最小値=赤」(ユーザー指定による)
- テキスト属性: 識別性の高い20色パレット+超過分は黄金角(137.5°)で色相を回して
  自動生成(色数無制限・重複なし)。割り当ては出現数順。
  「上位7カテゴリのみ色分け」オプションで残りをグレーの「その他」に集約できる
  (人間が色で区別できるのは8色程度という原理的限界への対応)
- カスタムは開始色・終了色に加え**中間色**を挟んだ3色補間に対応(例: 赤→白→青。既定色は開始=赤、終了=青)
- **反転**チェックで全配色の向きを逆転できる
- **範囲の手動指定**: 最小・最大を固定できる(意図: 外れ値で色が潰れる対策、
  複数モデル間で色の基準を揃える用途。RectAngleを±45固定+赤白青にすると
  傾き方向が対称に可視化できる)
- **手動範囲は属性ごとに保持する**(`manualRanges`)。範囲は属性に固有の意味を
  持つため、1組しか持たないと属性を切り替えたとき前の範囲が残る。
  例えばWidthの 0.27〜0.86 を残したままTilt(実際は67〜76)に切り替えると、
  全ての値が範囲外に振り切れて**全オブジェクトが同じ色**になり、
  凡例にも前の属性の範囲が出るため原因に気づけない。
  属性の切り替え時に、前の属性の値を退避してから新しい属性の値を復元する
  - **モデルを読み直したら破棄する**(`disposeModel`)。範囲は元データに固有の
    値であり、別のデータへ持ち越すと同じ問題が起きるため
  - 入力のたびに退避している。切り替え時にまとめて拾おうとすると、
    色分けの再適用との順序で取りこぼす
- **よく使う属性には初期値を持たせる**(`DEFAULT_RANGES`)。毎回同じ基準で
  グラデーションを描けるようにし、別モデルとの比較で色の意味を揃える。
  値は実際の調査で扱う常識的な範囲に基づく

  | 属性 | 初期値 | 根拠 |
  |---|---|---|
  | Width / Height | 0.1〜1.5 | 極端に大きい/小さい石で色が潰れないようにする |
  | Area | 0.05〜1 | 実データの主要な範囲。おすすめフィルタの下限と揃える |
  | FillRate | 0.5〜0.9 | 0〜1の値だが実データは中央付近に集まる |
  | Aspect | 0.25〜4 | 1をまたぐ範囲(縦長・横長の両方を含む) |
  | Tilt | 30〜90 | 90が定義上の最大(垂直な面) |
  | RectAngle | -45〜45 | 定義上の範囲。中間色を白にすると左右対称に見える |
  | SelfIntersect | 0〜0.02 | 大半が0のため自動範囲だと外れ値1個で潰れる。おすすめフィルタのしきい値(0.01)が中央に来る |

  - 上記以外の属性は空欄(自動)から始まる
  - **空欄も「利用者の選択」として記録する**。空欄を「未設定」として削除すると、
    既定値のある属性で自動に戻したくても、切り替えて戻るたびに既定値が
    復活してしまい自動にできなくなる
- **「範囲を手動で指定」は既定でオン**。誰が開いても同じ配色になり、
  書き出したSVGを並べて比較できるようにするため
  - 範囲からはみ出す値がある場合は、凡例のヒストグラムの端が高くなるため
    利用者が気づける(範囲外の値は端のビンに積み上がる)。警告は出さない
  - ただし**最小>最大で範囲が成立しない場合**は、全て同じ色になるうえ
    ヒストグラムも山にならず、原因に気づけない。この場合のみ確認ダイアログを出す
    (`showRangeDialog`)

### 6.1.1 範囲が成立しないときの扱い

最小値が空欄(自動)で最大値だけ既定値という組み合わせでは、データが既定値を
大きく超えると vmin > vmax になる(例: 幅3〜9.1mのデータに上限1.5m)。

- **黙って自動範囲に切り替えない**。利用者が指定した設定が勝手に変わるのは
  許容しない方針のため
- 代わりに**確認ダイアログで選ばせる**
  - 「データに合わせる」→ 入力欄をデータの実際の最小・最大に**書き換える**。
    何が起きたか画面上で見えるようにするため、内部だけで切り替えることはしない
  - 「指定のままにする」→ 承知のうえで指定どおり描く。その属性は覚えておき、
    再適用のたびに聞き直さない(モデルを読み直すと忘れる)
- 選択が済むまで着色しない(中途半端な状態を見せない)
- **設定変更は即時反映**(一度実行した後)。カラーピッカーは150msデバウンス。
  再適用時はマテリアルを作り直さず色だけ更新する(1万個規模への配慮)

### 6.2 表示技術

- オブジェクトごとにMeshStandardMaterialを持たせ、色分け時は色のみ更新
- 凡例(左下): 属性名+件数、**ヒストグラム(24ビン、各ビンを実際の
  グラデーション色で着色)**、グラデーションバー、最小・最大値。
  ヒストグラムをバーの真上に置くのは「どの色のオブジェクトが何個あるか」を
  直接読めるようにする意図
- ラベル: 各オブジェクトの重心位置にHTML要素を毎フレーム投影配置。
  複数属性の同時表示、文字サイズ調整、**距離カリング**(遠いラベルを自動非表示。
  ラベル過密と描画負荷の両方への対策)
- クリック(タップ)で属性インスペクタ表示(右上)

### 6.3 フィルタ

- **任意の数値属性 × 範囲(最小/最大)** の条件を複数追加できる(AND)。
  当初はArea/Flatness固定だったが、「Tiltでも絞りたい、他の属性でも」という
  要望を受けて汎用化した
- 自己交差は専用のチェックボックスではなく、**通常の条件として指定する**
  (§5.1.1で0〜0.5の実数になったため。「交差の有無」ではしきい値を表現できず、
  専用UIを残すと通常の条件と経路が二重になる。§6.3が Area/Flatness 固定から
  汎用化してきた流れに合わせ、仕組みを1つに揃えた)
- 除外の表現: **半透明(既定)/ 非表示**。半透明を既定にしたのは
  「何が除外されたかを確認しながらしきい値を調整できる」ため
- 除外オブジェクトはラベル・SVG書き出しからも除かれる
- **入力値は「残す範囲」**であり、範囲の外側が除外される。
  ラベルは当初「除外条件」だったが、これは**除外する範囲を入れる欄と読める**ため、
  意図と逆の値を入れる誤りが起きやすい(例: 細長い形を除くつもりで
  アスペクト比を 0.25〜0.5 にすると、細長いものだけが残る)。
  **「残す範囲(この範囲から外れたオブジェクトを除外)」**に変更した。戻さないこと

#### 属性が無くなった条件の扱い

CSVを差し替えると、条件に選んでいた属性が新しいデータに無いことがある。
このとき**別の属性に付け替えてはいけない**。選択肢を作り直すと先頭の属性が
自動的に選ばれ、数値だけが残るため「面積 0.05以上」が「別の属性 0.05以上」に
化ける。画面上は同じ数字が並んでいるので気づけず、全件が除外されることもある。

- 属性が無くなった条件は`orphaned`とし、「(この属性はありません)」を選択した
  状態で保持する(数値は残す)。`applyFilter`は属性が空の条件を無視する
- **何を解除したかを必ず知らせる**(§6.3のおすすめ設定と同じ考え方)
- 利用者が選び直したら`orphaned`を解除して通常の条件に戻す
- 新しく追加した条件は先頭の属性から始める。選択なしにすると、
  表示は先頭の属性なのに値は空という食い違いが起きる

### 6.3.1 色分けしていなくてもフィルタは使える

当初フィルタは色分けの実行を前提にしていた。属性値の紐づけ(`attributesByMesh`)も、
半透明にできるマテリアルも、`applyColorize`が用意していたためである。
そのため「色をリセット」するとフィルタも解除していた。

しかし**「形だけを絞り込んで見たい」**(色分けは要らない)という使い方があり、
色分けを前提にする必然性は無い。次の2点を色分けから独立させた。

- **属性値の紐づけ**: フィルタを使う直前に `buildAttributeMap()` で作り直す。
  色は変えず、IDの突き合わせだけを行う。**色分けの実行時に作られたものに頼らない**。
  頼ると、CSVを差し替えた後に色分けし直すまで古いCSVの値で絞り込んでしまう
  (画面には新しい列名の条件が出ているのに、評価しているのは前のデータ)
- **除外表示のマテリアル**: 色分け由来でないマテリアルは`dimmableMaterial()`で
  **複製してから**透明度を書き換える。読み込んだGLBのマテリアルは複数の
  オブジェクトで共有されていることがあり、直接触ると関係のないオブジェクトまで
  薄くなるため。元のマテリアルは`originalMaterials`に残るのでリセットで戻せる
  - 複製するのは除外対象だけ。全件を複製すると無駄が大きい

これに伴い、**「色をリセット」してもフィルタは解除しない**。おすすめ設定も
色分けなしで押せる。

#### おすすめフィルタ設定

調査で繰り返し使う絞り込みを1操作で入れられるようにしたもの(`FILTER_PRESET`)。

| 属性 | 残す範囲 | ねらい |
|---|---|---|
| Area | 0.05 以上 | 小さすぎる破片を除く |
| Aspect | 0.25 〜 4 | 極端に細長い形を除く |
| Tilt | 30 以上 | 水平に近い面を除く |
| SelfIntersect | 0.01 以下 | 輪郭が壊れたものを除く |

- **SelfIntersectの0.01は「Areaを約2%以上狂わせる交差」を意味する**(§5.1.1)。
  投影の折り返しで生じる程度の小さな交差(実測で面積比3%以下)は残す
- **Aspectの範囲が1をまたぐのが要点**。アスペクト比は幅÷高さなので、
  細長い形は1より十分小さい(縦長)か十分大きい(横長)かの両方に現れる。
  片側だけ指定すると一方しか除けない
- 値は実際の調査で使われている設定に基づく。データ依存の閾値であり、
  対象が変われば適切な値も変わる。**万能の設定ではない**
- 適用時は既存の条件を**置き換える**(足し込むと意図しない絞り込みになる)
- 読み込んだデータに無い属性は飛ばし、飛ばしたことを通知する
  (GLB+外部CSVでは属性が揃わないことがある)
- 色分けの実行は前提ではない(§6.3.1)。押せばそのまま絞り込みが始まる

### 6.4 傾きライン

- 各オブジェクトの**外接矩形の水平軸に沿った中心線**(長さ=Width、矩形中心を通る)を描画する
- 色は矩形回転角の**絶対値による対称グラデーション**(既定: 0°=青、±45°=赤。
  中央色・両端色ともピッカーで変更可)。「線が水平に揃っていれば整った積み方」という
  読み取りをさせる意図
- **太さは実寸mm指定(20〜100mm)**。WebGLはライン太さを変えられないため、
  太さを持つクワッド(三角形2枚)として描画する
- 石は完全な平面ではない(Flatness最大10cm程度)ため、面上に置くと線が凹凸に
  埋まって途切れる。**Flatness+4mmだけ面から浮かせる**ことで原理的に埋まらない
- 浮かせる向きは**壁全体の平均法線**(上向きルールで揃えて平均)に統一。
  輪郭の巻き方向が石ごとにバラバラでも裏側に出ない
- 名称は「通り線」→「回転角ライン」→**「傾きライン」**と変遷。初見の利用者に
  機能の本質が伝わることを優先した

### 6.5 選択ハイライト・テーマ・視点プリセット

- クリックした石は**オレンジの輪郭線**で強調(深度テスト無効=他の石に隠れても見える)
- ダーク/ライトテーマ切替(ビュー右上)。UIのCSS変数と3Dシーンの背景・グリッド色が連動
- 視点プリセット「正面」(立面図SVGと同じ投影方向)・「上面」(真上から見下ろす)・「全体」
- 全体フィットは**リセンタリング方式**: 距離の二分探索+画面内方向の平行移動で
  余白5%・均等配分を実現する(単純な対称フィットでは透視投影の非対称性により
  斜め視点で奥側に大きな余白が残るため。Blender等のCAD系ビューワーと同じ考え方)

### 6.6 投影方式の切り替え(透視投影 / 平行投影)

- **設計意図**: 透視投影は自然な見え方だが、奥行きによって見かけの大きさが変わるため
  寸法の比較や実測図的な見え方の確認には不向き。CAD系ビューワーの「平行投影」相当の
  モードを追加し、ワンクリックで切り替えられるようにした
- **実装**: `PerspectiveCamera`と`OrthographicCamera`の2台を常に保持し、`camera`変数が
  現在アクティブな方を指す。レイキャスト・投影(`Vector3.project`)・
  行列(`matrixWorldInverse`)などカメラ種別を意識しないコードはそのまま両対応する
- 切り替え時は**現在の視線方向(オービットの向き)を保ったまま**、その向きで
  改めてフィット(§6.5の全体フィットと同じ考え方)する。距離(透視)とズーム(平行)は
  基準が違うため、切り替えのたびに再計算が必要
- **ズームの扱いの違い**: 透視投影はカメラを近づける/遠ざける(オービット半径を変更)。
  平行投影は距離を変えても見た目が変化しないため、`camera.zoom`を変更する。
  ホイール・ピンチとも、操作しているカメラの種別で分岐する
- **平行投影のフィット計算**(`fitViewOrtho`): 透視投影の`fitView`(距離の二分探索)とは
  違い、パースがないため8頂点の投影後u/v座標はカメラ距離に依存しない。
  そのままu/v範囲(+余白)をフラスタムの上下左右に反映するだけでよく、二分探索は不要
- **「現在のビューをSVGで保存」との連動**(§8.4): この切り替えに連動して、
  平行投影のときは真の平行投影(パースなし)のSVGが書き出される
- **状態が分かりにくい問題への対応**: ボタンの文言・枠線色だけでは現在の投影方式が
  一見して分かりにくいというフィードバックを受け、ビュー右下に「平行投影」ラベルを
  追加した(透視投影は既定のため無表示、非既定の平行投影のときだけ主張する)。
  Blender(ビューポート隅に「User Persp」/「User Ortho」を常時表示)やRhino
  (各ビューポートに投影方式ラベルを常設)など、他の3D/CADツールの慣習に合わせた

### 6.7 軸ギズモ

- **設計意図**: 元データ(DXF等、Z-up)のX/Y/Z軸が現在の視点でどちらを向いているかを、
  視点を回転させるたびに直感的に把握できるようにする(Blenderのナビゲーションギズモ相当)
- ビュー右下に常時表示するSVG。X=赤・Y=緑・Z=青(Zは鉛直=データのZ軸)
- **実装**: `camera.quaternion`の逆回転を各軸の単位ベクトルに適用し、カメラのローカル
  座標系での向きを求める(位置・投影方式に依存しない、向きのみのギズモ)。
  ローカルZ成分(奥行き)でソートして奥から手前へ描画し、手前の軸が隠れないようにする
- **座標対応**: データX→Three.js X、データY→Three.js -Z、データZ(上)→Three.js Y
  (§3の「Z-up → Y-up」変換と対になっている。変換ロジックを変更した場合は
  `AXIS_GIZMO_DEFS`の向きベクトルも合わせて更新が必要)
- `orbit.update()`から毎回呼ばれるため、回転・パン・ズーム・視点プリセット・
  投影切り替えのすべてに自動的に追従する

---

## 7. ID自動採番

### 7.1 仕様

- DXF/サンプル読み込み時、**立面図ビュー(§8の投影)で左下のオブジェクトから
  順に 1, 2, 3... と自動採番**する(ボタン操作不要)
- 採番順: 下の段から、段内は左から右
- 段の判定: 投影後の重心高さでソートし、「重心高さの差がオブジェクト高さの
  中央値の60%以内」なら同じ段とみなすバンド分割(模擬データ6段×65石で
  正しく分離することを検証済み)
- 元のID(レイヤー名由来)は **SrcID属性として必ず保持**する。
  意図: 自動で振り直すことによるトレーサビリティ喪失の防止。CSVがそのまま
  新旧IDの対応表になる
- 段判定の結果は**Course(段)属性として保存**され、色分け・フィルタ・CSV出力に使える
- 手動の「IDを振り直す」ボタンも残す(フィルタ後の再採番など)
- GLB読み込みは自動採番の対象外(名前がCSVマッチングに使われるため)

---

## 8. 立面図SVG書き出し

### 8.1 投影の定義

- **全オブジェクトの面法線を平均し、水平化した方向から見た正射投影**
  (=壁の正面・水平視線からの正式な立面図。3Dビューのカメラ位置に依存しない)
- 検証: 水平方向の寸法は実寸のまま、高さは壁の後傾分だけ見かけ通りに縮む、
  という立面図の性質を満たすことを数値検証済み
- **表裏判定**: 「石垣の外向き法線は上向き(Z+)成分を持つ」という性質を利用し、
  各法線を上向き成分が正になるよう統一してから平均する。これにより最初の
  オブジェクトの頂点巻き方向に依存せず、常に表(外面)から見た図になる。
  法線がほぼ水平のオブジェクトのみ参照法線への揃えにフォールバック

### 8.2 出力内容

- 各オブジェクトの輪郭ポリゴン(現在の色分け色、細い輪郭線、奥→手前の描画順)
- 凡例(属性名+件数、ヒストグラム、グラデーションバー、最小・最大値)
- **スケールバー**(右下。モデル幅の約1/6に近い切りの良い長さを自動選択、単位m)
- **含める要素は書き出し設定で選択**(凡例 / スケールバー / ラベル / 傾きライン)。
  3Dビューの表示状態から独立させてあり、「3Dでは非表示だがSVGには入れる」ができる
- **輪郭線の太さはmm実寸指定**(既定10mm、0で輪郭線なし)
- **塗りつぶしなし(線画)モード**: 実測図・トレース図風の出力。凡例は自動で外れる
- 右下にアプリ名とバージョンを刻印(図版のトレーサビリティ)
- フィルタで除外したオブジェクトは出力しない
- 白背景・sans-serif。報告書・論文用にIllustrator等で後編集できる完全ベクター

### 8.2.1 書き出せるものが無いときのメッセージ

対象が0件になる原因は2つあり、区別して伝える。一律に
「DXF/サンプルから読み込んだモデルでのみ利用できます」と出すと、
DXFを読み込んでいる利用者が原因を探せない。

- 輪郭を持つメッシュが1つも無い → 対応形式の説明
- 輪郭はあるが**フィルタで全て除外されている** → フィルタが原因である旨

### 8.3 対象の制限

- **輪郭モデル(DXF/サンプル)のみ**。ジオメトリの頂点順序=輪郭であることを
  利用しているため(`mesh.userData.isContour`フラグで判定)。
  GLB(三角形メッシュ)と背景OBJは対象外

---

### 8.4 現在のビューSVG書き出し

#### 設計意図

- 立面図SVG(§8)は常に「壁の正面」に固定された正射投影で、構図を選べない。
  斜めから見た説明図など任意の見せ方をしたい用途には向かないため、
  現在のカメラの見た目をそのまま書き出すモードを追加した

#### 投影の定義

- 立面図と異なり、**現在アクティブなカメラ(透視/平行、§6.6)のprojectionMatrix/
  matrixWorldInverseをそのまま使う**(Three.jsの`Vector3.project(camera)`でNDCに変換)。
  `project()`はカメラ種別を意識せず使えるため、投影処理自体に分岐は不要で、
  透視投影ならパースあり、平行投影なら真のパースなしのSVGがそのまま得られる
- 近クリップ面(`camera.near`)より手前に頂点を持つ石は投影が破綻するため、
  クリッピングはせず**その石ごと除外**する(1頂点でも手前にあれば除外する簡易対応)
- 描画順は立面図と同じく奥→手前(カメラ空間のz値でソートするペインターズアルゴリズム)

#### 出力内容の差分(立面図との違い)

- **スケールバーはどちらの投影方式でも出力しない**: 透視投影は距離により見かけの
  縮尺が変わるため、平行投影も(モード切り替えの分かりやすさを優先し)一律で対象外とした
- 輪郭線・傾きラインの太さ(mm実寸→px変換)は、透視投影はオービット注視点
  (`orbit.target`)での見かけの縮尺、平行投影は`camera.zoom`と可視高さ
  (`camera.top - camera.bottom`)から一定の縮尺を求める(距離に依存しないため近似不要)
- 凡例(ヒストグラム・グラデーションバー/カテゴリ)・ラベル・傾きラインの描画ロジックは
  立面図と共通化した(`buildLegendSVGParts()`)。書き出し設定パネルも共有する

#### 対象の制限

- §8.3と同様、輪郭モデル(DXF/サンプル)のみが対象

---

### 8.5 SVG書き出しの共通化(§8と§8.4の関係)

立面図SVG(§8)と現在のビューSVG(§8.4)は、**投影の求め方だけが異なり
それ以外は同一**である。当初は2つの関数にコードが約150行重複しており、
SVG機能に手を入れるたびに二重修正が必要だった。以下の構造に整理した。

| 関数 | 役割 |
|---|---|
| `readSVGExportOptions()` | 書き出し設定パネルの読み取り |
| `buildExportSVG(items, view, opt)` | SVG本体の組み立て(ポリゴン・傾きライン・ラベル・凡例・スケールバー・版情報) |
| `buildLegendSVGParts()` | 凡例部分(先行して共通化済み) |
| `downloadSVG(text, filename, message)` | ファイル保存とステータス表示 |

**投影の違いは `view` オブジェクトに閉じ込める**。各書き出し関数は
自分の投影方法で `items[].pts`(抽象座標)を作り、以下を渡すだけでよい。

- `toX(x) / toY(y)` — 抽象座標からSVGのpx座標への変換
- `pxPerUnit` — 実寸1単位あたりのpx(線の太さのmm実寸指定に使う)
- `projectLine(rl)` — 傾きラインの端点を抽象座標へ。描けない場合は `null`
  (現在のビューでは近クリップ面より手前の線がこれに当たる)
- `scalebarSpan` — スケールバーの基準にする横幅。**`null` なら描かない**。
  縮尺が一定な正射投影(立面図)でのみ意味を持つという判断をここに集約している

#### 変更時の注意

この2つの書き出しは自動テストの対象外(DOM・THREE依存のため)。
変更したら**リファクタ前後でSVG出力が一致することを確認する**こと。
共通化の際は、設定4パターン×2方式+平行投影の計9通りで
出力の長さとハッシュが完全一致することを確認した。

---

### 8.6 データ名(Dataset列)

- ステップ1の「データ名」欄で設定する。ファイル読み込み時にファイル名(拡張子なし)が
  自動セットされ、**手動編集済みの場合は上書きしない**(自動セット値と一致する場合のみ更新)
- CSV書き出し時、名前が入力されていれば**全行にDataset列(末尾)として付与**する。
  想定用途は複数調査データのBIツール・pandasでの結合分析における由来識別
- 書き出したCSVを読み戻すとDataset列から名前欄に復元される(往復で名前が保持される)
- CSV出力はRFC 4180準拠のエスケープを行う(カンマ・引用符・改行・日本語を含む
  値でも壊れない。横断分析の前提となるデータ品質の保証)

---

### 8.6.1 凡例は色分けの状態に従う

凡例の内容(`lastLegendState`)は色分けを実行したときに作られる。
**色をリセットしたら捨てる**こと。残していると、灰色一色に戻した図を
書き出したときに前回の属性のグラデーション凡例だけが付き、
図と説明が食い違う。まとめ書き出しの後始末で色分けなしの状態へ戻す場合も同じ。

### 8.7 まとめて書き出す(ZIP)

報告書には「幅で色分けした図」「面積で色分けした図」…と複数枚が必要になる。
1枚ずつ属性を切り替えて保存するのは手間が大きいため、選んだ属性の立面図を
一度に書き出して1つのZIPにまとめる。

### 8.7.1 ZIPを自前で書く理由

- **外部ライブラリを追加しない**という方針(§2)を守るため、JSZip等は使わない。
  ブラウザ標準の `CompressionStream("deflate-raw")` でdeflate圧縮し、
  ZIPのヘッダ・中央ディレクトリ・CRC32は自前で組み立てる(約80行)
  - SVGはテキストなので圧縮が効く。実測で **31KB → 9KB(29.7%)**
  - 7枚で約66KB。個別ダウンロードなら約220KB
- `CompressionStream` が無い環境では**無圧縮(store)で格納**する。
  サイズは縮まないが、ZIPとしては正しく開けるため
  「ファイルが散らばらない」という主目的は果たせる
- ファイル名にデータ名(日本語を含みうる)が入るため、名前はUTF-8で書き、
  汎用フラグのbit11を立てる。Windowsの標準機能(Expand-Archive)で
  日本語名のまま展開できることを確認済み
- **ファイル名に使う文字は必ず`safeFileNamePart`を通す**。データ名だけでなく
  **CSVの列名から作る部分にも通すこと**。列名は利用者のファイル由来で
  任意の文字を含み、`幅/高さ`のような列名はZIPの中でフォルダになり、
  `..`を含む列名は展開先の外へ書き出されうる
  - 記号だけの列名は整形すると空になるため、その場合は連番で代用する

### 8.7.2 個別ダウンロードにしない理由

ブラウザは複数ファイルの連続ダウンロードに対して確認を出し、拒否や抑制が
起きると途中で止まる。枚数が増えるほど失敗しやすく、
ダウンロードフォルダも散らかる。ZIPなら1ファイルで完結する。

### 8.7.3 対象と既定値

- **立面図に限る**。見比べるための機能であり、向きと縮尺が揃っている必要がある。
  現在のビューは視点次第で縮尺が変わるため対象にしない
- 既定でチェックする属性は Width / Height / Area / FillRate / Aspect / Tilt。
  **RectAngle(矩形回転角)は既定で外す**。傾きラインの線画が同じ情報を
  より読み取りやすい形で示すため、色分け図と二重になる。
  重心座標(X/Y/Z)や元IDなど、色分けしても読み取れない列も外れる
- 「傾きラインの線画」は塗りつぶしなし + 傾きライン + 凡例なしで書き出す。
  塗らないので色分けは関係なく、色分けをやり直す必要もない。
  凡例は色を説明するものなので外す

### 8.7.4 実行中の状態管理

- 属性を次々に切り替えて着色するため、**書き出しの前後で画面の状態を必ず戻す**。
  属性・範囲の入力欄・色分け済みかどうかを退避し、`finally`で復元する。
  元々色分けしていなかった場合は元のマテリアルに戻す
- 範囲が成立しない属性(最小>最大)があっても、**ダイアログは出さない**(`batchRunning`)。
  属性ごとに手が止まってしまうため。その属性だけ飛ばし、
  **どれを飛ばしたかを最後にまとめて知らせる**
  - 黙って自動範囲に切り替えないという§6.1.1の方針は変えていない。
    設定を変えずに「今回は書き出さなかった」と伝える形にしている
- `applyColorize()` は成否を真偽値で返す。まとめ書き出しが
  「着色できたかどうか」で分岐する必要があるため

---

## 9. その他の書き出し

- **CSV**: 現在の属性テーブル(自動計算 or 外部CSV)をBOM付きUTF-8で保存
- **ID付きGLB**: 表示中のメッシュを、名前(ID)を保持した標準GLBとして自前で
  組み立てて書き出す。**法線の向き(三角形の巻き順)は壁平均法線に統一**してから
  出力する(元データの巻き方向はバラバラなため。表示には影響しない書き出し時のみの処理)。
  往復テスト(書き出し→自前パーサーで読み戻し)で名前・座標の保持を検証済み。**DXF→GLB変換**としても機能し、凍結したBlenderアドオン
  ワークフローへの接続点でもある

---

## 10. UI構成

サイドパネルは7ステップのアコーディオン(ヘッダークリックで開閉)。
初期展開は必須の「1. モデル」「3. 色分け」のみ(意図: iPadでの縦長パネル対策と、
DXFワークフローでは2が不要なため)。

1. **モデルを読み込む** — GLB/DXF選択、データ名、フォトグラメトリ背景(ファイル複数選択 or
   フォルダ選択)、背景の不透明度・表示・削除
2. **属性データを読み込む(任意)** — CSV選択、ID列名、接頭辞、ID振り直しボタン。
   DXF読み込み時は「✓ 自動計算した属性を使用中」と表示され、飛ばしてよいことを示す
3. **色分けする** — 属性選択(選ぶと定義の説明を表示)、配色、反転、詳細設定(範囲手動指定・カテゴリ数)
4. **傾きラインを描画する(任意)** — 表示切替、線の色(0°/±45°)、太さ
5. **フィルタで絞り込む(任意)** — 残す範囲(複数)、おすすめ設定、自己交差除外、表示方法
6. **ラベルを表示する(任意)** — 表示内容(複数選択)、文字サイズ、距離カリング
7. **書き出す** — CSV / ID付きGLB / 立面図SVG / 現在のビューSVG / まとめてZIP

**折りたたみ(`details`)の既定**

- ステップ3の「詳細設定(範囲の固定・カテゴリ数)」は**既定で開く**。
  v1.9.0で範囲を既定で固定するようにしたのに、その値が折りたたまれていて
  見えなかったため。何を基準に色が付いているかは常に見えている必要がある
- ステップ7の「まとめて書き出す」は、**ボタンを折りたたみの外に出す**。
  折りたたみの中にあると書き出し手段があること自体に気づけない。
  他の書き出しボタンと同じ並び・同じ見た目(`ghost`)に置き、
  対象の属性を選ぶ部分だけを折りたたみに残す
  - ボタンの色は変えない。`action`(アクセント色)はステップの主操作に限る
    という使い分け(§10.5)を崩さないため

### 10.1 見出しと必須/任意の表し方

- **連番は「上から順にやること」と読まれる**ため、実際には飛ばせるステップ(2・4・5・6)には
  見出しに**(任意)**を付ける。これがないと「7. 書き出す」が「6まで終えないと使えない」
  と誤読される。必須の流れは 1 → 3 → 7
- 初期展開(1・3)と(任意)表記は必ず一致させる。片方だけ変えると「任意なのに最初から
  開いている」といった矛盾が生じる

### 10.2 文字サイズの階層

主張の強い順に4段階。同じ役割には同じサイズを使う。

| サイズ | 用途 |
|---|---|
| 16px | アプリ名(h1) |
| 13px | 入力欄・選択欄・主アクションボタン(=操作対象) |
| 12px | ステップ見出し(太字・アクセント)、ステータス、副次ボタン |
| 11px | サブ見出し(太字・アクセント)、フィールドラベル、補足文 |

- サブ見出しとフィールドラベルは同じ11pxだが、**太字+アクセント色**で上下関係を示す
  (かつてサブ見出しを10.5pxにしていたが、親が子より小さいのは階層として逆であり、
  0.5px差は知覚できず不揃いにしか見えなかったため揃えた)

### 10.3 補足文の2種類

| クラス | 位置 | 役割 |
|---|---|---|
| `.section-note` | セクション冒頭(コントロールより前) | このステップが何か・使うべきかの説明 |
| `.hint` | 直前のコントロールの直後 | そのコントロール個別の補足 |

意図: 「説明が前にあったり後ろにあったり」に見える状態を避ける。
前後の位置そのものに意味を持たせ、クラス名で使い分けを明示する。

その他:
- ステータス表示はパネル最上部(結果・警告をスクロールなしで見せる)。
  アプリの主要なフィードバック手段なので補足文より一段大きい12px・本文色。
  等幅フォントは日本語で別フォントにフォールバックし字面が乱れるためsansを使う
- サンプル表示中はビューポート左上にバッジ表示(ボタン文言を状態表示に
  使わない。操作=ボタン、状態=バッジという役割分担)
- 操作: 左ドラッグ回転 / 右ドラッグ・Shift+ドラッグ・2本指ドラッグでパン /
  ホイール・ピンチでズーム / クリックで属性表示

### 10.4 タッチ操作への配慮

- 開閉ヘッダー(ステップ見出し・詳細設定)とチェック行は、文字高だけだと14〜16pxしかなく
  タップしづらいため、上下paddingで**当たり判定を24px以上**確保する
  (WCAG 2.2の最小ターゲットサイズ。iPad対応を掲げている以上ここは外せない)
- **色見本の意味をtitle属性(ホバー)だけで説明しない**。タッチ環境にホバーは存在せず、
  何の色か永久に分からなくなるため、`.swatch`で見本の下に文字ラベルを併記する

### 10.5 ボタンの見た目の使い分け

| 見た目 | 意味 | 例 |
|---|---|---|
| 塗りつぶし(アクセント) | そのステップの主アクション。**画面に1つだけ** | 色分けを実行(未実行時) |
| 輪郭線のみ | 副次的な操作。状態で色は変えない | 色をリセット、各種書き出し |
| 破線 → 実線+✓ | ファイルの未読込 → 読込済み(状態表示) | ファイル選択ボタン |

- **実行ボタンは状態に応じて格下げする**: 未実行なら塗りつぶし(唯一の主役)、
  実行後は設定変更が自動反映されるためボタンは不要になるので、輪郭線に落として
  文言も「色分けを再実行」に変える。起動時はサンプルが色分け済みなので、
  格下げがないと「まだ何か押す必要がある」と誤解させてしまう
- 逆に**トグルボタンの色で現在の状態を示さない**(§6.6)。文言=遷移先、色は一定に保ち、
  現在の状態は専用のインジケータで示す。文言と色が別々の意味を持つと混乱する

### 10.6 表示設定の保存(localStorage)

「毎回チェックを入れ直すのが手間」という要望に応えるもの。ただし
**保存するのは作業環境の好みだけ**に絞る。

#### 保存しないもの(意図的な除外)

- **配色の基準**(配色プリセット・反転・カスタム色・範囲の指定・カテゴリ数)。
  §6.1の「誰が開いても同じ配色になる」という前提が崩れるため。
  保存すると、本人には「前回の続き」に見えたまま他の人と色の意味がずれ、
  **ずれていること自体に気づけない**。利便性より比較可能性を優先する
- **データに紐づくもの**(データ名・ID列・接頭辞・対象属性・フィルタの条件)。
  次に開くデータでは意味を持たない
- **表示のオン/オフ**(傾きライン・ラベル・背景モデル)。
  対象が読み込まれていない状態で復元しても意味がない

#### 保存するもの

背景の不透明度、傾きラインの色と太さ、ラベルの文字サイズ・表示距離・距離カリング、
除外の表現(半透明/非表示)、SVG書き出しの含める要素と輪郭線の太さ、
まとめ書き出しに線画を含めるか、テーマ、投影方式。

SVG書き出しの設定は出力の見た目を変えるが、**色の意味は変えない**ため保存対象に含める。

#### 実装上の注意

- キーは `gac.settings.v1`。**接頭辞とバージョンを付ける**。GitHub Pagesは
  `https://reisyu.github.io` というオリジン全体で保管領域を共有し、
  パスでは分離されないため、他のプロジェクトと衝突しうる
  - ローカルで開いた場合(`file://`)とは別の保管領域になる。公開版とローカル版で
    設定は共有されない
- 読み書きは**必ずtry/catchで囲む**。プライベートモードや容量超過では例外になるが、
  設定が保存できなくてもアプリは動くべきである。壊れたJSONや型の違う値、
  知らないキーがあっても既定値で起動する
- 復元後にイベントを発火させるのは、値の表示(「50mm」など)や連動する状態を
  更新する必要があるものだけ。書き出し設定は使うときに読まれるので不要
  - 「塗りつぶしなし」だけは例外的に手で整合を取る。changeを発火させると
    塗りありのときに凡例を強制的にオンへ戻してしまうため
- 保存はスライダー操作のたびに書かないよう300msまとめる(`scheduleSaveSettings`)
- 変更を保存するリスナーの登録は復元処理とは分け、**1度だけ**行う

#### リセット用のUIは用意しない

保存される値はすべて画面上の項目から直接変えられ、操作すればそのまま覚え直す。
詰まる状態が無いため、専用のリセットは不要と判断した。

---

## 11. 内蔵サンプル

- 起動時に石垣サンプル(123オブジェクト)を色分けまで済んだ状態で表示する
  (意図: 初見の人が開いた瞬間に何ができるアプリか伝わるようにする)
- **実際の石垣調査データを使う**。同梱の `sample_ishigaki.dxf` から輪郭座標だけを
  抜き出し、`SAMPLE_CONTOURS` として index.html に埋め込んでいる
  - DXF全文(約390KB)ではなく座標のみ(約24KB)を持つ。単一HTMLファイルの方針(§2)を
    保ちつつ肥大を避けるため。座標はmm精度に丸めてある
  - 更新は `node tools/embed_sample.js` で再生成する
- 属性計算はDXFと同じ経路(computeContourAttributes)を通す。サンプル自体が
  計算ロジックの動作デモを兼ねる

### 11.1 手続き生成から実データへ差し替えた理由

当初は乱数シード固定で石垣風の形状を生成していた(6段・整った矩形・後傾18°)。
しかし**理想化された形のため属性の分布が実際の石垣とかけ離れており**、
§6.1の範囲の初期値(実データに基づく値)と噛み合わなかった。

具体的には、生成サンプルは幅が0.27〜0.86mしかなく、初期値の上限1.5mに対して
グラデーションの6割程度しか使えず色が眠くなっていた。実データに差し替えた結果、
幅は0.06〜1.29mとなり、初期値を適用しても色の広がりは自動範囲とほぼ同等
(0.807 対 0.807)になった。

**サンプルは「動作デモ」であると同時に「既定値が妥当かを確かめる基準」でもある。**
初期値を変更する場合は、このサンプルで色が潰れないかを確認すること。

---

## 12. 公開・運用

- リポジトリ: `geometry-attribute-colorizer`(GitHub)、MIT License
- 公開: GitHub Pages(`https://reisyu.github.io/geometry-attribute-colorizer/`)
- 同梱物: `index.html` / `help.html` / `README.md` / `LICENSE` /
  `sample_ishigaki.dxf` / `assets/`(READMEの画像)
- Blenderアドオン版(`CSV_Colorize_Addon`)は凍結。READMEに移行の旨を記す方針

### 12.1 バージョン管理

セマンティックバージョニング(メジャー.マイナー.パッチ)に従う。判定基準は
CHANGELOG.md 冒頭に記載。

**バージョン表記は4箇所に重複している。**

| 場所 | 用途 |
|---|---|
| `index.html` の `APP_VERSION` | 画面表示、SVG出力への刻印 |
| `help.html` の `HELP_VERSION` | 操作ガイドの見出し・フッター |
| `SPECIFICATION.md` の「対象バージョン」 | この文書が対象とする版 |
| `CHANGELOG.md` の最新見出し | リリース記録 |

1箇所にまとめられないのは、**「index.html 1つで完結」(§2)を守るため**。
共通のJSファイルに切り出すと単一ファイル配布が崩れ、ビルドで埋め込む方式は
ビルド不要という前提そのものを壊す。重複を受け入れる代わりに、
**`tests/test_version.js` が4箇所の一致を機械的に検証する**。

実際にv1.1.0のとき仕様書だけ1.0.0のまま取り残された事故があった。
このチェックを外さないこと。

### 12.2 リリース手順

1. 変更を実装し、`node tests/run_all.js` が全て通ることを確認する
2. 4箇所のバージョンを上げ、CHANGELOG.mdに項目を追加する
   (テストがバージョン一致とCHANGELOGの並び順を検証する)
3. コミットする
4. **タグを打つ**: `git tag -a v1.7.0 -m "v1.7.0: 要約"`
5. **ブランチとタグを分けてpushする**(順番も重要)
   ```
   git push origin main      # 先にブランチ。これでPagesのビルドが起動する
   git push origin v1.7.0    # タグは後から送る
   ```
   `--follow-tags` で一度に送ると**Pagesのビルドが起動しない**(§12.4)
6. **GitHubでReleaseを作成する**(下記§12.3)
7. **公開サイトが更新されたか確認する**(§12.4)。
   pushしてもPagesのビルドが起動しないことが実際に起きているため、
   反映を目視で確かめるところまでがリリース

### 12.2.1 起動できないときの案内

起動に必要なものが揃わないと、**画面自体は普通に表示されるのに何も動かない**
状態になる。サンプルが出ず、ファイルを読ませても無反応で、利用者は原因を
知る手がかりが無い。実際に2つの経路で再現している。

| 欠けるもの | 起きる場面 |
|---|---|
| CDNの外部ライブラリ | オフライン、`cdnjs.cloudflare.com` のブロック、通信の途中断 |
| WebGL | ハードウェアアクセラレーション無効、リモートデスクトップ・仮想環境、古い端末 |

どちらも `showStartupError()` で原因と対処を示して止める。
**新たに起動時の前提を増やすときは、同じように「揃わなかったとき」を用意すること。**
静かに壊れると、講習会のような場面で全員が同時に詰まる。

### 12.2.2 オフライン配布版

外部ライブラリをCDNから読み込む構成のため、**会場のネットワークが使えないと
起動できない**。しかも画面は普通に表示されるので、利用者は原因が分からない。
対策は2つに分けている。

- **公開版**: 読み込みに失敗したら、原因と対処を示す画面を出して止める。
  よくある原因(オフライン / `cdnjs.cloudflare.com` のブロック / 通信の途中断)まで書く
- **配布版**: `node tools/build_offline.js` でライブラリを埋め込んだ単一HTMLを作る。
  `dist/` に操作ガイドと練習用DXFも一緒に出力するので、フォルダごと渡せば
  ネットワーク無しで動く

配布版を公開版にしない理由は、ライブラリを更新するたびに作り直しが必要になり、
公開サイトと配布物で中身がずれるため。**講習会など、事前に配る用途に限る**。

### 12.3 タグとReleaseの違い

**この2つは別物で、タグを push しても Releases 欄には並ばない。**

| | 実体 | 作り方 | 表示場所 |
|---|---|---|---|
| **タグ** | Gitのref | `git tag` + push | Tagsタブ |
| **Release** | GitHub独自のオブジェクト | GitHubの画面かAPIで作成 | Releasesタブ |

タグだけでも「その版のコードを取り出す」用途は満たせる。
Releaseにすると、説明文・配布用ZIPのリンクが付き、
**どれが最新版かが外から分かる**(Latestバッジ)。

#### Releaseの作り方

1. `https://github.com/reisyu/geometry-attribute-colorizer/releases/new?tag=v1.3.0`
   を開く(`?tag=` に既存のタグ名を指定すると選択済みで始まる)
2. **Choose a tag** が目的のタグ(既存)になっていることを確認する
3. **Release title** に `v1.3.0: 要約` を入れる
4. 説明にCHANGELOG.mdの該当バージョンの内容を貼る
   (**Generate release notes** で自動生成もできるが、コミット一覧が並ぶだけなので
   CHANGELOGを整形して貼るほうが読みやすい)
5. **Set as the latest release** にチェックが入っていることを確認して **Publish release**

`gh` CLI があれば `gh release create v1.3.0 --notes-file ...` でも作れる。

#### 経緯

- v1.0.0以降しばらくタグを打っておらず、v1.1.0〜v1.3.0の6件を後から補完した
  (各バージョンを導入したコミットに注釈付きタグを付与)
- Releaseはv1.0.0のみ手動作成されていた。過去分を遡って作る必要性は薄い
  (タグが残っていれば取り出せるため)。**最新版だけReleaseにすれば足りる**


### 12.4 pushしたのに公開サイトが更新されないとき

**タグを含むpushでは、GitHub Pagesのビルドが起動しない。**
§12.2のとおりブランチとタグを分けて送れば起きないが、
pushが成功していても公開サイトが古いままになることがあるため、
**リリース後は公開サイトのバージョン表記を必ず確認する**こと。

#### 判別

リポジトリの **Actions** タブで、該当コミットの
「pages build and deployment」の実行記録を探す。

| 状態 | 意味 | 対処 |
|---|---|---|
| runが**失敗**している | ビルドエラー | ログを見て原因を修正 |
| runが**存在しない** | 起動の取りこぼし | 下記の再トリガー |
| runが**成功**しているのに古い | CDNキャッシュ | 数分待つ / Ctrl+F5 |

公開サイトのバージョンは `CHANGELOG.md` をそのまま開くと確認しやすい
(`https://reisyu.github.io/geometry-attribute-colorizer/CHANGELOG.md`)。
index.htmlのバージョンはJSで描画されるため、ソースを見ても分からない。

#### 再トリガーの方法(推奨順)

1. **Settings → Pages で Source を一度 `None` にし、`main / (root)` に戻す**
   — コミットを増やさずに済む。実際にこれで復旧した実績がある(v1.4.0)
2. 空コミットをpushする — 確実だが履歴に残る
   ```
   git commit --allow-empty -m "GitHub Pagesの再デプロイをトリガー" && git push
   ```

**Actionsの「Re-run all jobs」は効かない。**
古いコミットを再デプロイするだけで、新しい内容は反映されない。

#### 経緯

pushの仕方との対応が実測で確認できている。

| 版 | pushの仕方 | ビルド |
|---|---|---|
| v1.3.1 / v1.4.0 / v1.5.0 | `--follow-tags`(タグ同時) | **3回とも起動せず** |
| v1.6.0 〜 v1.12.2 | ブランチ → タグを別送 | **9回とも自動起動** |

- v1.3.1では8時間経っても記録が現れなかった。Actionsは有効で過去28回すべて
  成功しており、設定の問題ではなかった。空コミットで復旧した
- v1.4.0・v1.5.0はSourceの切り替えで復旧した
- v1.6.0からブランチとタグを分けて送るようにしたところ、手動介入なしで
  公開まで通っている。§12.2の手順を守れば、この節の対処は不要になる

---

## 13. テスト体制

### 方式

- テストは Node.js で実行する(`node tests/run_all.js`)。外部依存なし
- `extract_for_tests.js` が **index.html から対象関数をそのまま切り出して**
  `tests/extracted.js` を生成し、各テストはこれを `require` する。
  意図: テスト用にロジックを書き写すと実装と乖離しても気づけないため、
  **必ず実コードを検証する**

### 絶対に守ること

1. **`run_all.js` の冒頭で `extract_for_tests.js` を必ず実行する**(自動化済み)。
   以前は再生成を手動運用に任せていたため、`normalizeId` が常に `"BROKEN"` を
   返すようにアプリを破壊しても**94件全てが成功する**状態だった。
   古い `extracted.js` を検証していたことが原因。この自動化を外さないこと
2. **テストファイル内にアルゴリズムの写しを置かない**。
   かつて `test_fitview.js` と `test_winding.js` が写しを検証しており、
   index.html をどう変更しても永久に成功する状態だった。
   THREE や DOM に依存して抽出できない場合は、**依存のない純粋部分を
   index.html 側で関数として切り出す**(`solveFitDistance` /
   `solveFitOrtho` / `flipTriangleWinding` はこの目的で分離したもの)。
   テスト側に置いてよいのは、カメラ基底の組み立てのような
   THREE が担う「接続部分」の再現までに留める

### 変更時の確認手順

アプリのロジックを変更したら、意図的に壊して**テストが失敗することを確認する**
(失敗しないなら、そのコードは検証されていない)。

---

## 14. 既知の課題・今後の検討事項

- 統計の拡張(平均・分散の数値表示、2属性の散布図)
- 複数データセットの比較表示(修理前後、壁面間)
- ラスター(PNG)書き出し(背景モデル込みのプレゼン用画像)
- 多言語対応(英語UI)
- index.htmlの肥大化(現状5,300行弱・243KB。単一ファイル配布の利点を優先して
  継続中だが、開発時分割+ビルドで単一HTML生成する体制も将来の選択肢)
- (解消済み)SVG書き出し2関数の重複は §8.5 の共通化で解消した
- 100行超の関数が7個(`loadDXFFile` 170行、`loadOBJSet` 170行、
  `computeContourAttributes` 146行、`applyColorize` 145行、`exportGLB` 143行、
  `parseDXF` 107行、`renumberIds` 104行)。
  動作リスクに対して分割の見返りが薄いため優先度は低い
- テスト対象は104関数中22関数(幾何・色計算・DXF読み込み・ZIP書き出しに集中)。
  UI・3D描画・OBJ読み込みの結合部分は手動確認に依存している
- 平行投影のズームインに上限がない(透視投影は距離に下限クランプがある)。
  「全体」で復帰できるため実害は小さい
- 検証スクリプト(開発時にPythonで行った幾何計算の検証)のリポジトリ同梱
